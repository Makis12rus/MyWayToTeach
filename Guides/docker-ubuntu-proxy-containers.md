# 🚀 РУКОВОДСТВО: Твоя Личная Армия Серверов Ubuntu с Docker! 🚀

Йоу, сорванец! 👋 Готов прокачаться и перестать переживать, что твои проги глючат, потому что у них нет своего личного пространства? Хочешь замутить что-то реально крутое, чтобы каждая приложуха сидела в своём уютном уголке и никого не трогала? 🏡

А что, если тебе нужно много-много одинаковых мини-серверов Ubuntu 22.04 для всяких тестов, чтобы каждый был как настоящий отдельный компьютер со своим секретным туннелем (SOCKS5) и возможностью тайного входа (SSH)? Тогда ты попал по адресу! 😉

### Что такое Docker и почему это твой личный чит-код? 🎮

Представь, что твой компьютер — это огромная комната, и раньше все твои программы толпились в ней, постоянно устраивая драмы и ссоры из-за места (ну, ты понимаешь, как бывает 🙄).

С Docker Desktop всё по-другому! Это твой личный **"чит-код"**. Вместо одной общей комнаты, мы дадим каждой проге свой собственный, чёткий **"домик"** — его мы называем **контейнером** 📦. Помни, каждый такой контейнер — это твой **изолированный мини-сервер Ubuntu 22.04!** Круто, да? 🤩

А знаешь что самое крутое? У нас есть ещё и главный стратег — **Docker Compose** 🎩. Он поможет тебе сразу построить целые "районы" таких домиков и разрулить, кто с кем дружит, чтобы твои приложения могли расти и тащить по-крупному! 🚀

Так что, пристегни ремни! Мы начинаем наше цифровое приключение! ✨

---

### Раздел 1: Устанавливаем Твой Командный Центр (Docker Desktop) 🛠️

Прежде чем начать строить свою цифровую империю, нам нужно подготовить "базу" на твоём компьютере. Это как подготовить стол, прежде чем строить LEGO! 🧱

1.  **Если у тебя компьютер Windows (ну, как у большинства 😉)**
    * **1.1 Загрузи установщик:** Отправляйся на [официальный сайт Docker](https://www.docker.com/products/docker-desktop/) и скачай установочник для Windows. Он будет весить немало, так что запасись терпением и, может быть, вкусняшками. ⏳ Файл будет называться что-то вроде `Docker Desktop Installer.exe`.

    * **1.2 Установка шаг за шагом:**
        * Запусти файл. Нажимай "Далее", "Согласен" (ну, ты знаешь, как это бывает с программами), пока не увидишь вот такое окошко **"Configuration"** (Настройки):

                    * **Что тут важно:**
            * **"Use WSL 2 instead of Hyper-V (recommended)"**: Это твоя секретная база 🤫 внутри компа, где будут жить твои контейнеры. Без неё никак, так что **ОБЯЗАТЕЛЬНО ставь галочку ✅!** (WSL 2 — это как маленький, но очень мощный Linux внутри твоего Windows).
            * **"Allow Windows Containers to be used with this installation"**: Эту галочку **НЕ ТРОГАЙ ❌**. Нам нужны контейнеры Linux (типа Ubuntu), так что не будем отвлекаться на Windows-версии.
            * **"Add shortcut to desktop"**: По желанию, если хочешь, чтобы иконка Docker была прямо на рабочем столе. 🖥️ Это удобно!
        * Жми "OK". Готово! Ты получишь установленный Docker Desktop и готовую базу для своих цифровых "домиков".

    * **1.3 Первый запуск: Docker тебя приветствует!**
        * Как только откроешь Docker в первый раз, он начнет парить мозг с приветствием и всякими турами. Можешь просто нажать **"Skip" (Пропустить)** в правом верхнем углу, чтобы сразу перейти к делу. 🚀
        * * **Что ты увидишь**: Docker Desktop откроется, готовый к работе.
        * Ты получишь: Возможность сразу рулить Docker, без лишнего "приветственного" гемора.

    * **1.4 База готова к работе!**
        * После всех этих "церемоний" откроется главное окно Docker Desktop.
        * **Что ты увидишь и что делать:**
            * **Слева:** Меню твоей империи: "Containers" (Твои домики), "Images" (Шаблоны домиков). Пока пусто, но скоро будет движ! 🧸
            * **В центре:** Тут будут видны все твои запущенные домики.
            * **Внизу:** Должно быть **"Engine running" (Движок работает) 🟢**. Это значит, что твой командный центр готов к работе.
        * **Что нажимать?** Пока ничего! 😊 Все команды мы будем отдавать через **Терминал** (это то самое чёрное окошко, куда ты пишешь команды, как волшебные заклинания 🧙‍♂️). А это окно Docker Desktop — просто твой монитор, где ты можешь следить за тем, что происходит.
        * Ты получишь: Запущенное приложение Docker Desktop, готовое к строительству империи!

2.  **ОЙ! Если не запускается (вот это облом 😱):**
    Бывает такое, не паникуй! Вот что можно попробовать, если Docker Desktop вдруг "закапризничал":
    * **2.1 WSL2 завис?** Открой "Командную строку" от имени администратора (это важно! 👑) и напиши:
        ```bash
        wsl --set-default-version 2
        ```
        Возможно, нужно просто обновить WSL.
        * **Что ты увидишь**: Сообщение о том, что операция завершена.
    * **2.2 Виртуализация в BIOS/UEFI:** Убедись, что она включена. Это как движок, без которого ни один твой цифровой "домик" не поедет. Погугли "как включить виртуализацию на [модель твоего компа]", это обычно делается в настройках, когда ты включаешь компьютер. 🧠
    * Ты получишь: Все возможности для запуска виртуальных машин.

3.  **Если у тебя компьютер macOS (ну, понты, чё 😎)**
    * **3.1 Загрузи установщик:** Забирай установочник с [официального сайта Docker](https://www.docker.com/products/docker-desktop/), выбирай свою версию macOS. Ты получишь `.dmg` файл.
    * **3.2 Установка:** Перетащи его в "Applications" ➡️, как обычную прогу.
        * Ты получишь: Docker Desktop в папке приложений.
    * **3.3 Запуск:** Запускай Docker Desktop! Возможно, попросит разрешения. Увидишь что-то про HyperKit (это как секретная база для Mac).
        * Ты получишь: Запущенный Docker Desktop.

4.  **😂 ОЙ! Если HyperKit тупит (Docker над тобой ржёт 🤭):**
    * **4.1 Перезагрузи Mac.** 🔄 Серьёзно, иногда это самое эффективное решение для многих проблем!
    * **4.2 Настройки безопасности:** Возможно, Mac не пускает HyperKit. Зайди в "Системные настройки" -> "Безопасность и конфиденциальность" и дай разрешение.
    * Ты получишь: Разрешение для HyperKit, чтобы он мог создать свою секретную комнату.

---

### Раздел 2: Строим Свою Армию Серверов Ubuntu (Мультиаккинг — это круто!) 🏗️

Так, чувак, хватит париться с одним "домиком"! Мы тут не за этим. Сегодня мы сразу замутим целую сеть одинаковых домиков Ubuntu 22.04, каждый из которых будет работать как полноценный VPS-сервер, со своим уникальным SOCKS5-прокси для выхода в интернет и возможностью тайного входа (SSH). 🌐🔒

Наш главный стратег **Docker Compose** 🎩 поможет нам собрать весь этот движ: много одинаковых серверов, и всё это в своей собственной, чёткой сети.

1.  **Наш план: Строим армию серверов с Docker Compose 🚀**
    * **Зачем это вообще надо? (Стратегия и мощь!) ✨**
        Представь, что у тебя не один сервер, а целая **банда абсолютно одинаковых мини-серверов Ubuntu 22.04**, которые должны работать как часы. Каждый такой "домик" — это твой отдельный, изолированный мини-сервер, готовый выполнять любые задачи тестирования!
        Ты сможешь запускать на них что угодно — от Python-скриптов до нод блокчейнов, и каждый будет иметь свой уникальный "отпечаток" в интернете благодаря отдельному SOCKS5-прокси. Это как иметь много разных масок в интернете! 🎭
        Запускать всё это вручную — жуткий гемор. Реальный облом. 😩 Но **Docker Compose** — твой босс-стратег. Он прочитает твой план (`docker-compose.yml`) и запустит весь этот движ одним махом! 🤯

    * **Термины для крутых чуваков:**
        * **Сервис** — это как название для одного типа домика в нашем плане (например, "тестовый\_сервер\_убунту"). В `docker-compose.yml` ты определяешь сервисы, а Docker Compose создает **контейнеры** (то есть, отдельные мини-серверы) для каждого сервиса.
        * **Контейнер** — это сам запущенный "домик". Помни, каждый контейнер — это отдельный, изолированный мини-сервер Ubuntu 22.04, готовый к работе. У каждого свой уникальный IP-адрес 📍 в твоей внутренней сети.
        * **Сеть** — это твои личные, секретные "дороги" 🏝️ между домиками-мини-серверами, чтобы они могли общаться, не светясь наружу.

2.  **Создаем "Рецепт" для наших Домиков (Dockerfile) 🍰**
    Прежде чем строить домики-мини-серверы, нужен их "рецепт". Это файл `Dockerfile`, который расскажет Docker, как "испечь" образ (шаблон) для наших домиков с Ubuntu, установить SSH-сервер, `proxychains` и настроить их. Все твои домики-серверы будут сделаны по этому одному рецепту.

    **Создай папку `мои_тестовые_серверы_docker`, а внутри неё — файл `Dockerfile` (без расширения!) и закинь туда вот это:**
    ```dockerfile
    # =========================
    # 📝 Файл: Dockerfile
    # =========================
    # Базовый образ: Мы начинаем с чистой Ubuntu 22.04
    FROM ubuntu:22.04

    # Обновляем пакеты и устанавливаем нужные программы:
    # openssh-server - чтобы можно было подключаться по SSH
    # curl - для проверки IP и других сетевых штук
    # net-tools, iproute2 - для работы с сетью
    # proxychains - ГЛАВНОЕ: это поможет нам использовать прокси!
    RUN apt update && \
        apt install -y openssh-server curl net-tools iproute2 proxychains && \
        mkdir -p /var/run/sshd # Создаём место для работы SSH

    # 🚨 ВНИМАНИЕ: Секретный пароль для главного пользователя root! 🚨
    # ЗАМЕНИ <ТВОЙ_СЕКРЕТНЫЙ_SSH_ПАРОЛЬ> на РЕАЛЬНО СЛОЖНЫЙ пароль!
    # Это ОЧЕНЬ ВАЖНО для безопасности. Никогда не используй простые пароли типа '123'!
    RUN echo 'root:<ТВОЙ_СЕКРЕТНЫЙ_SSH_ПАРОЛЬ>' | chpasswd

    # Эти настройки упрощают SSH в Docker для обучения, но снижают безопасность.
    # На реальных серверах так делать не стоит!
    RUN sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
    RUN sed -i 's/PermitEmptyPasswords no/PermitEmptyPasswords yes/' /etc/ssh/sshd_config
    RUN sed -i 's/UsePAM yes/UsePAM no/' /etc/ssh/sshd_config

    # Объявляем, что наш домик будет "слушать" на порту 22 для SSH-звонков.
    EXPOSE 22

    # Копируем наш скрипт запуска start.sh в контейнер
    # И делаем его исполняемым, чтобы он мог запускаться
    COPY start.sh /usr/local/bin/start.sh
    RUN chmod +x /usr/local/bin/start.sh

    # Используем ENTRYPOINT для запуска скрипта настройки proxychains.
    # Это гарантирует, что скрипт всегда выполняется при старте.
    # Переменные окружения будут доступны здесь.
    ENTRYPOINT ["/usr/local/bin/start.sh"]

    # CMD по умолчанию, если скрипт не запускает длительный процесс.
    # SSHD будет запущен в start.sh, поэтому эта CMD не будет выполняться,
    # но это хорошая практика для отладки.
    CMD ["/usr/sbin/sshd", "-D"]
    ```
    **ОБЯЗАТЕЛЬНО ЗАМЕНИТЕ `<ТВОЙ_СЕКРЕТНЫЙ_SSH_ПАРОЛЬ>` на свой собственный, ОЧЕНЬ СЛОЖНЫЙ пароль!** ⚠️ Это твоя безопасность, не забудь! Он должен быть сложным, как шифр древних майя! 🧐
    * **Что ты увидишь**: Множество строк текста в терминале, показывающих процесс сборки образа, установки пакетов и настройки. В конце — сообщение об успешном создании образа.
    * Ты получишь: Готовый "рецепт" для всех твоих будущих домиков-серверов с предустановленным SSH-сервером и `proxychains`. Теперь Docker будет знать, как их "печь", чтобы они могли принимать SSH-подключения и автоматически использовать прокси.

3.  **Создаем скрипт запуска (`start.sh`) 🚀**
    В той же папке (`мои_тестовые_серверы_docker`), где у тебя лежит `Dockerfile`, создай новый файл с именем `start.sh` и вставь в него следующий код. Этот скрипт будет динамически генерировать файл `proxychains.conf` внутри каждого контейнера, используя уникальные данные прокси из переменных окружения.

    ```bash
    #!/bin/bash
    # =========================
    # 📝 Файл: start.sh
    # =========================

    # Этот скрипт настраивает proxychains.conf динамически
    # и активирует автоматическое использование proxychains через LD_PRELOAD
    # для интерактивных сессий. Также запускает SSH-сервер.

    # Извлекаем данные прокси из переменной ALL_PROXY
    # Пример: socks5://user:pass@host:port или socks5://host:port
    PROXY_FULL_STRING="${ALL_PROXY}"

    # Удаляем "socks5://" префикс
    PROXY_CLEAN="${PROXY_FULL_STRING#socks5://}"

    # Разделяем строку на части для аутентификации и хоста/порта
    PROXY_HOST_PORT=""
    PROXY_USERNAME=""
    PROXY_PASSWORD=""

    if [[ "${PROXY_CLEAN}" == *"@"* ]]; then
        # Если есть @, значит есть user:pass (логин и пароль)
        AUTH_PART="${PROXY_CLEAN%@*}"
        PROXY_HOST_PORT="${PROXY_CLEAN#*@}"
        PROXY_USERNAME="${AUTH_PART%:*}"
        PROXY_PASSWORD="${AUTH_PART#*:}"
    else
        # Нет @, значит нет user:pass (менее вероятно для твоих прокси)
        PROXY_HOST_PORT="${PROXY_CLEAN}"
    fi

    # Извлекаем хост (IP или домен) и порт
    PROXY_HOST="${PROXY_HOST_PORT%:*}"
    PROXY_PORT="${PROXY_HOST_PORT#*:}"

    # Создаем файл конфигурации proxychains
    # strict_chain - значит, что трафик пойдет только через прокси
    # proxy_dns - DNS-запросы тоже пойдут через прокси
    cat <<EOF > /etc/proxychains.conf
    strict_chain
    proxy_dns
    # Указываем DNS-серверы, которые proxychains будет использовать через прокси
    nameserver 8.8.8.8
    nameserver 1.1.1.1
    tcp_read_time_out 15000
    tcp_connect_time_out 8000

    [ProxyList]
    EOF

    # Добавляем строку с прокси в зависимости от наличия аутентификации
    if [[ -n "$PROXY_USERNAME" ]]; then
        echo "socks5 ${PROXY_HOST} ${PROXY_PORT} ${PROXY_USERNAME} ${PROXY_PASSWORD}" >> /etc/proxychains.conf
    else
        # Если прокси без аутентификации (например, открытый прокси)
        echo "socks5 ${PROXY_HOST} ${PROXY_PORT}" >> /etc/proxychains.conf
    fi

    # Убедимся, что proxychains.conf имеет правильные разрешения
    chmod 644 /etc/proxychains.conf

    # Создаем скрипт в /etc/profile.d/ для автоматической настройки proxychains при входе
    # Это будет применяться для всех интерактивных оболочек (например, при SSH-подключении)
    cat <<EOF > /etc/profile.d/proxychains_auto.sh
    #!/bin/bash
    # Экспортируем LD_PRELOAD, чтобы proxychains автоматически перехватывал сетевые вызовы.
    # Путь к библиотеке proxychains может зависеть от архитектуры (x86_64-linux-gnu, arm-linux-gnueabihf и т.д.)
    # dpkg-architecture -qDEB_BUILD_MULTIARCH определяет правильный путь.
    export LD_PRELOAD=/usr/lib/\$(dpkg-architecture -qDEB_BUILD_MULTIARCH)/libproxychains.so
    # Указываем proxychains, где находится его конфигурационный файл
    export PROXYCHAINS_CONF=/etc/proxychains.conf
    # Необязательно: можно убрать стандартные HTTP_PROXY/HTTPS_PROXY,
    # чтобы избежать конфликтов или если вы хотите, чтобы только proxychains рулил трафиком.
    # unset HTTP_PROXY HTTPS_PROXY ALL_PROXY NO_PROXY
    EOF

    # Делаем скрипт исполняемым
    chmod +x /etc/profile.d/proxychains_auto.sh

    # Запускаем SSH-сервер как основной процесс (PID 1)
    # Это гарантирует, что контейнер будет оставаться запущенным,
    # пока работает SSH-сервер.
    exec /usr/sbin/sshd -D
    ```
    * **Что ты увидишь**: Создание файла `start.sh` и изменения его прав на исполняемые.
    * Ты получишь: Скрипт, который будет автоматически генерировать правильный `proxychains.conf` для каждого контейнера при его запуске, используя уникальные данные прокси из переменных окружения. Это как дать каждому домику свой личный навигатор! 🗺️

4.  **"Испеки" свой образ домика! 🍪**
    После того как ты создал файлы `Dockerfile` и `start.sh` со всеми этими инструкциями, тебе нужно сказать Docker "испечь" из него образ. Это как сделать шаблон для печенья, которое потом можно много-много раз выпекать! 😋

    Открой **Терминал** (убедись, что ты находишься в той же папке `мои_тестовые_серверы_docker`, где лежит твой `Dockerfile` и `start.sh`!) и набери:
    ```bash
    docker build -t my-ubuntu-ssh-server .
    ```
    * `docker build` — это команда, которая говорит Docker: "Начинай строить образ!"
    * `-t my-ubuntu-ssh-server` — это как дать имя своему шаблону. Это имя поможет тебе потом легко найти этот образ.
    * `.` (точка) в конце — это очень важно! Она говорит Docker: "Ищи `Dockerfile` и другие файлы, например, `start.sh`, в текущей папке."
    💡 **Важно!** Имя твоего образа должно быть только в **нижнем регистре!** Используй строчные буквы, цифры и дефисы (`-`) или подчеркивания (`_`). **НИКАКИХ ЗАГЛАВНЫХ БУКВ!** Это как правила написания ников в играх — всё по-маленькому! 🎮
    * **Что ты увидишь**: Docker начнет выполнять все команды из `Dockerfile` по очереди. Ты увидишь много текста в Терминале, это Docker устанавливает пакеты, копирует скрипт и настраивает SSH. Если всё пройдёт успешно, в конце ты увидишь сообщение об успешном создании образа.
    * Ты получишь: Теперь у тебя есть готовый шаблон, по которому Docker Compose будет создавать твои мини-серверы! 🎉

5.  **Пишем "план строительства" (`docker-compose.yml`) 📝**
    В той же папке (`мои_тестовые_серверы_docker`), где у тебя уже есть `Dockerfile` и `start.sh`, создай или обнови файл `docker-compose.yml`. Это будет твой мега-план для строительства целой инфраструктуры с уникальными SOCKS5-прокси для каждого сервера! 🏰

    Мы определим несколько абсолютно одинаковых сервисов, каждый из которых будет использовать наш `my-ubuntu-ssh-server` образ, иметь свой уникальный SOCKS5-прокси и проброшенный SSH-порт.
    **Важно:** Для каждого тестового сервера тебе понадобится свой уникальный SOCKS5-прокси, а также правильные логин и пароль.

    ```yaml
    version: '3.8' # Указываем версию Docker Compose

    services:
      # Первый тестовый сервер Ubuntu
      # Он будет использовать СВОЙ УНИКАЛЬНЫЙ SOCKS5-прокси
      <ИМЯ_СЕРВИСА_1>: # Придумай классное имя, например, "web_spider_1" или "node_alpha"
        build: . # Указываем, что Docker Compose должен использовать Dockerfile в текущей папке
        container_name: ubuntu_<ИМЯ_СЕРВИСА_1> # Имя контейнера, чтобы легко его находить
        ports:
          - "2221:22" # Пробрасываем порт SSH: 2221 на твоём компе ведёт на 22 внутри контейнера
        networks:
          <ИМЯ_ВАШЕЙ_СЕТИ>: # Указываем, к какой сети подключается наш домик
            ipv4_address: 172.20.0.10 # Даём ему уникальный IP-адрес внутри нашей сети
        environment: # Здесь мы передаём секретные данные прокси в контейнер
          # ПРАВИЛЬНЫЙ ЛОГИН И ПАРОЛЬ для первого прокси
          HTTP_PROXY: "socks5://<ЛОГИН_ПРОКСИ_1>:<ПАРОЛЬ_ПРОКСИ_1>@<IP_АДРЕС_ПРОКСИ_1>:1080"
          HTTPS_PROXY: "socks5://<ЛОГИН_ПРОКСИ_1>:<ПАРОЛЬ_ПРОКСИ_1>@<IP_АДРЕС_ПРОКСИ_1>:1080"
          ALL_PROXY: "socks5://<ЛОГИН_ПРОКСИ_1>:<ПАРОЛЬ_ПРОКСИ_1>@<IP_АДРЕС_ПРОКСИ_1>:1080"
          # NO_PROXY - куда не нужно ходить через прокси (локальные адреса Docker)
          NO_PROXY: "localhost,127.0.0.1,host.docker.internal,docker-desktop,172.17.0.0/16,172.18.0.0/16,172.19.0.0/16,172.20.0.0/16"
        dns: # DNS-серверы для контейнера
          - 8.8.8.8 # Google DNS
          - 1.1.1.1 # Cloudflare DNS
        restart: always # Если домик "упал", пусть Docker его перезапустит автоматически

      # Второй тестовый сервер Ubuntu
      # Он будет использовать СВОЙ УНИКАЛЬНЫЙ SOCKS5-прокси
      <ИМЯ_СЕРВИСА_2>: # Придумай классное имя, например, "web_spider_2" или "node_beta"
        build: .
        container_name: ubuntu_<ИМЯ_СЕРВИСА_2>
        ports:
          - "2222:22"
        networks:
          <ИМЯ_ВАШЕЙ_СЕТИ>:
            ipv4_address: 172.20.0.11 # Уникальный IP для этого домика
        environment:
          # ПРАВИЛЬНЫЙ ЛОГИН И ПАРОЛЬ для второго прокси
          HTTP_PROXY: "socks5://<ЛОГИН_ПРОКСИ_2>:<ПАРОЛЬ_ПРОКСИ_2>@<IP_АДРЕС_ПРОКСИ_2>:1080"
          HTTPS_PROXY: "socks5://<ЛОГИН_ПРОКСИ_2>:<ПАРОЛЬ_ПРОКСИ_2>@<IP_АДРЕС_ПРОКСИ_2>:1080"
          ALL_PROXY: "socks5://<ЛОГИН_ПРОКСИ_2>:<ПАРОЛЬ_ПРОКСИ_2>@<IP_АДРЕС_ПРОКСИ_2>:1080"
          NO_PROXY: "localhost,127.0.0.1,host.docker.internal,docker-desktop,172.17.0.0/16,172.18.0.0/16,172.19.0.0/16,172.20.0.0/16"
        dns:
          - 8.8.8.8
          - 1.1.1.1
        restart: always

      # Третий тестовый сервер Ubuntu
      # Он будет использовать СВОЙ УНИКАЛЬНЫЙ SOCKS5-прокси
      <ИМЯ_СЕРВИСА_3>: # Придумай классное имя, например, "web_spider_3" или "node_gamma"
        build: .
        container_name: ubuntu_<ИМЯ_СЕРВИСА_3>
        ports:
          - "2223:22"
        networks:
          <ИМЯ_ВАШЕЙ_СЕТИ>:
            ipv4_address: 172.20.0.12 # Уникальный IP для этого домика
        environment:
          # ПРАВИЛЬНЫЙ ЛОГИН И ПАРОЛЬ для третьего прокси
          HTTP_PROXY: "socks5://<ЛОГИН_ПРОКСИ_3>:<ПАРОЛЬ_ПРОКСИ_3>@<IP_АДРЕС_PROX_3>:1080"
          HTTPS_PROXY: "socks5://<ЛОГИН_ПРОКСИ_3>:<ПАРОЛЬ_ПРОКСИ_3>@<IP_АДРЕС_ПРОКСИ_3>:1080"
          ALL_PROXY: "socks5://<ЛОГИН_ПРОКСИ_3>:<ПАРОЛЬ_ПРОКСИ_3>@<IP_АДРЕС_ПРОКСИ_3>:1080"
          NO_PROXY: "localhost,127.0.0.1,host.docker.internal,docker-desktop,172.17.0.0/16,172.18.0.0/16,172.19.0.0/16,172.20.0.0/16"
        dns:
          - 8.8.8.8
          - 1.1.1.1
        restart: always

    networks: # Здесь мы описываем нашу личную сеть
      <ИМЯ_ВАШЕЙ_СЕТИ>: # Название нашей сети
        driver: bridge # Тип сети
        ipam: # Настройки IP-адресов
          config:
            - subnet: 172.20.0.0/16 # Диапазон IP-адресов для нашей сети
    ```
    **ОБЯЗАТЕЛЬНО замени** `<ЛОГИН_ПРОКСИ_X>`, `<ПАРОЛЬ_ПРОКСИ_X>` на **РЕАЛЬНЫЕ, ТОЧНЫЕ** логины и пароли для каждого уникального SOCKS5-прокси! 🔑 Каждый сервер должен иметь свой собственный прокси для уникальности. А `<IP_АДРЕС_ПРОКСИ_X>` на реальные IP-адреса твоих прокси. `<ИМЯ_СЕРВИСА_X>` замени на те имена, которые ты хочешь использовать (например, `web_spider_1`, `node_alpha` и т.д.). **Это супер-важно!** ⚠️
    * **Что ты увидишь**: Твой файл `docker-compose.yml` будет сохранён со всеми настройками.

6.  **Проверяем "план строительства" (`docker-compose config`) 🧐**
    Прежде чем запускать всю свою армию серверов, давай убедимся, что твой "план строительства" (`docker-compose.yml`) написан без ошибок и Docker Compose его точно поймёт. Для этого есть секретная команда:
    ```bash
    docker-compose config
    ```
    * `docker-compose config` — это как дать Docker Compose прочитать твой план, но без запуска домиков. Он просто проверит синтаксис YAML и покажет тебе финальную конфигурацию, которую он собирается использовать.
    * **Что ты увидишь**:
        * Если всё хорошо, ты увидишь на экране твой `docker-compose.yml` в его полном, обработанном виде. Это значит, что Docker Compose понял твой план, и ты готов к следующему шагу! ✅
        * Если есть ошибки (например, с отступами или синтаксисом), Docker Compose выдаст тебе сообщение об ошибке, указывая на проблемное место. Это как "красная карточка" от судьи, чтобы ты исправил косяк, пока не стало поздно! 💔
    * Используй эту команду каждый раз, когда меняешь `docker-compose.yml`, чтобы избежать сюрпризов при запуске! 🕵️‍♂️

7.  **Запускаем всех жителей по плану! ▶️ (с помощью Docker Compose)**
    Теперь, когда у тебя есть план (`docker-compose.yml`), и ты проверил его с помощью `docker-compose config`, пора дать команду нашему супер-стратегу Docker Compose! Открой **Терминал** (убедись, что ты всё ещё находишься в папке `мои_тестовые_серверы_docker`!) и напиши:
    ```bash
    # Если у тебя Windows (возможно, путь будет другим, проверь!)
    cd C:\Users\ТВОЁ_ИМЯ\Desktop\мои_тестовые_серверы_docker

    # Или если у тебя macOS / Linux
    cd ~/Desktop/мои_тестовые_серверы_docker

    # А теперь самое интересное!
    docker-compose up -d
    ```
    * `docker-compose up` — это твое магическое заклинание 🪄. Он читает твой план и начинает собирать, настраивать и запускать все сервисы, которые ты там прописал!
    * `-d` — это флаг "работай в фоне" (detached mode). Он говорит Docker Compose: "Запускай всё тихо, пусть работает, а ты можешь делать свои дела в Терминале!". 🤫
    * **Что ты увидишь**: Docker Compose автоматически сделает кучу всего:
        * "Испечёт" шаблоны домиков (`my-ubuntu-ssh-server`).
        * Создаст твою приватную сеть `<ИМЯ_ВАШЕЙ_СЕТИ>`.
        * "Поставит" домики `ubuntu_<ИМЯ_СЕРВИСА_1>`, `ubuntu_<ИМЯ_СЕРВИСА_2>`, `ubuntu_<ИМЯ_СЕРВИСА_3>` (и так далее), каждый со своим уникальным IP-адресом и проброшенным портом для SSH.
        * Настроит для каждого домика свой уникальный SOCKS5-прокси для выхода в интернет.
        * Ты увидишь сообщения о запусках.
    * Чтобы проверить, что все домики "живут" и работают, используй `docker ps`. Это как посмотреть на своих питомцев в вольере! 🐾

---

### Раздел 3: Секретный Доступ к Твоим Серверам (SSH и Проверка SOCKS5) 🔑

Теперь, когда твои мини-серверы Ubuntu запущены, давай разберемся, как к ним подключаться и как проверить, что их секретные SOCKS5-туннели работают, как часы! 🕵️‍♂️

1.  **Подключаемся к домику через SSH (Для ПРО!) 🧑‍💻**
    Чтобы подключиться к одному из своих тестовых серверов через SSH, тебе нужен SSH-клиент (это может быть PuTTY на Windows, или просто Терминал на Mac/Linux).
    Каждый твой тестовый сервер имеет свой уникальный проброшенный SSH-порт, который ты указал в `docker-compose.yml`.
    В Терминале (на твоём большом компьютере) напиши:
    * **Для `<ИМЯ_СЕРВИСА_1>`** (например, `python_test_node_1`):
        ```bash
        ssh root@localhost -p 2221
        ```
    * **Для `<ИМЯ_СЕРВИСА_2>`** (например, `python_test_node_2`):
        ```bash
        ssh root@localhost -p 2222
        ```
    * **Для `<ИМЯ_СЕРВИСА_3>`** (например, `blockchain_node_main`):
        ```bash
        ssh root@localhost -p 2223
        ```
    * **Что означают эти команды:**
        * `ssh` — это команда для SSH-подключения.
        * `root@localhost` — `"root"` это имя пользователя внутри домика (главный хозяин, который может делать всё!). `"localhost"` — это твой большой компьютер, через который мы подключаемся к контейнеру, так как порты 2221, 2222 и т.д. с твоего компа ведут на 22 внутри контейнера.
        * `-p <ПОРТ_НА_ТВОЕМ_КОМПЕ>` — указываем порт, на который мы перенаправили SSH-звонки.
    * Тебя попросят ввести пароль (тот самый, который ты указал в `Dockerfile` для `<ТВОЙ_СЕКРЕТНЫЙ_SSH_ПАРОЛЬ>`). **Введи его!** 🤫
    * **Что ты увидишь**: Ты окажешься внутри домика Ubuntu, как будто ты сидишь прямо перед ним! 🎉 Теперь ты можешь отдавать ему команды!

2.  **Подключение через MobaXterm (Для тех, кто любит по-богатому! 💎)**
    Если ты хочешь использовать MobaXterm – это крутая программа, которая делает жизнь намного проще, объединяя в себе много инструментов. Вот как к ней подключиться:
    * **2.1 Запусти MobaXterm.** Найди её на рабочем столе или в меню "Пуск".
    * **2.2 Создай новую сессию.** Нажми на кнопку "Session" в левом верхнем углу (или `Ctrl + Shift + N`).
            * **2.3 Выбери тип сессии "SSH".**
    * **2.4 Заполни поля:**
        * **Host:** `localhost` (помни, мы подключаемся к твоему компу, а Docker перенаправляет дальше).
        * **Specify username:** Поставь галочку и введи `root`.
        * **Port:** Введи номер порта для нужного тебе сервера:
            * Для `<ИМЯ_СЕРВИСА_1>`: `2221`
            * Для `<ИМЯ_СЕРВИСА_2>`: `2222`
            * Для `<ИМЯ_СЕРВИСА_3>`: `2223`
                    * Нажми "OK". MobaXterm попытается подключиться.
    * **2.5 Введи свой пароль.** Когда MobaXterm запросит, введи `<ТВОЙ_СЕКРЕТНЫЙ_SSH_ПАРОЛЬ>` (тот, что ты указывал в `Dockerfile`).
    * **Что ты увидишь**: Откроется окно терминала MobaXterm, где ты будешь сидеть прямо внутри своего мини-сервера Ubuntu!
    * Ты получишь: Удобный доступ к контейнеру. Ты можешь сохранить сессии, чтобы не вводить данные каждый раз! 💾

3.  **Проверяем, что Весь Трафик Идет Через SOCKS5-прокси Автоматически! 🤔**
    Теперь, когда `proxychains` установлен и настроен через `LD_PRELOAD` внутри каждого контейнера, **весь исходящий трафик** для большинства программ должен автоматически идти через твой SOCKS5-прокси.
    * **3.1 Внутри каждого контейнера (после подключения по SSH):**
        * Посмотри содержимое файла `proxychains.conf` для проверки, что он правильно сгенерировался с уникальными данными прокси:
            ```bash
            cat /etc/proxychains.conf
            ```
            * **Что ты увидишь**: В секции `[ProxyList]` одну строку `socks5` с IP-адресом, логином и паролем, которые соответствуют этому конкретному контейнеру. Если что-то не так, перепроверь `start.sh` и переменные в `docker-compose.yml`.
        * Теперь выполни команду `curl ifconfig.me` (БЕЗ `proxychains` в начале!):
            ```bash
            curl ifconfig.me
            ```ifconfig.me` — это простой сайт, который показывает твой внешний IP-адрес.
            * **Что ты увидишь**: Внешний IP-адрес, который **соответствует уникальному SOCKS5-прокси, назначенному именно этому контейнеру!** Например, для `<ИМЯ_СЕРВИСА_1>` это будет `<IP_АДРЕС_ПРОКСИ_1>`, для `<ИМЯ_СЕРВИСА_2>` — `<IP_АДРЕС_ПРОКСИ_2>`, а для `<ИМЯ_СЕРВИСА_3>` — `<IP_АДРЕС_ПРОКСИ_3>`. **Если IP-адреса разные — УРА, всё работает как надо! 🎉**

4.  **Как "свалить" из домика: 🚶‍♂️**
    Когда наиграешься и закончишь свои дела внутри контейнера, просто напиши:
    ```bash
    exit
    ```
    * **Что ты увидишь**: Ты вернёшься в свой обычный Терминал на компе.
    * Ты получишь: Твой "домик" продолжит работать в фоне, пока ты его не "вырубишь" (команды `docker stop` или `docker-compose down`).

---

### Раздел 4: Когда что-то пошло не так (Частые "ой!") 🐛

Даже у легенд бывают промахи, а у крутых программистов — тем более! 🏆 Главное — знать, как их вырулить и не паниковать! Вот самые частые "затыки" и как их решить:

1.  **`docker: command not found` 🚫**
    Это как если бы ты пытался позвать своего бро, а ты забыл его ник или он вне зоны доступа! 📵 Компьютер просто не понимает, что такое "docker".
    * **Как исправить:**
        * **1.1 Твой Docker Desktop вообще запущен?** ⏰ Главный движок Docker должен работать, чтобы ты мог отдавать команды. Посмотри внизу окна Docker Desktop, там должно гореть **"Engine running" (Движок работает) 🟢**. Если нет, запусти его!
        * **1.2 Docker вообще "на карте"? (Переменная PATH на Windows).** 🔍 При установке Docker Desktop он обычно сам "прописывается" в системную переменную PATH (это как список дорог, по которым компьютер ищет программы). Если этого не произошло, придётся руками добавить путь к его файлам, чтобы Терминал знал, где его искать. Попроси взрослого или погугли "как добавить Docker в PATH на Windows", если что.
    * Ты получишь: Возможность рулить Docker из любого места в Терминале.

2.  **`Network is unreachable` 📶**
    Твой контейнер пытается "дозвониться" куда-то, но сеть "глушит"! 🔌 Как будто он в бункере без связи.
    * **Как исправить:**
        * **2.1 Проверь свою тайную игровую комнату (WSL2 или HyperKit).** 🔄 Убедись, что она запущена и не тупит. Иногда эти "секретные базы" могут "зависнуть". Попробуй перезапустить Docker Desktop.
        * **2.2 Перезагрузи комп.** 💻 Да, иногда самый простой, но действенный "перезагрузка мозга" 🧠 помогает решить сетевые проблемы. Звучит глупо, но работает!
    * Ты получишь: Исправную сеть для Docker, и твои контейнеры смогут "звонить" куда угодно.

3.  **`Permission denied` 🙅**
    Это как если ты пытаешься войти в клуб, а тебя не пускает вышибала! 🚫 У тебя нет прав на выполнение команды.
    * **Как исправить:**
        * **3.1 На Windows:** Запусти Терминал от имени "главного" администратора. 👑 Щелкни правой кнопкой мыши по значку Терминала и выбери "Запуск от имени администратора". Это даст тебе все полномочия!
        * **3.2 На macOS / Linux:** Попроси взрослого или погугли, как добавить твоего пользователя в "команду Docker". 🤝 Обычно Docker Desktop делает это сам при установке. Но если что, нужно добавить твоего пользователя в специальную группу `docker`, чтобы ты мог отдавать команды без `sudo` (типа "Дайте мне все права!").
    * Ты получишь: Достаточные права для выполнения всех Docker-команд.

4.  **`/bin/sh: 1: [python3]: not found` (или аналогичная ошибка для другой программы) 🐍**
    Эта штука означает, что твой "домик"-контейнер не нашел нужную программу внутри себя или что-то неправильно понял. 🤷‍♀️ Часто бывает, когда ты пишешь "рецепт" (`Dockerfile`) в Windows, а он потом запускается на Linux-базе. У них разные "правила" для переноса строчек!
    * **Как исправить:**
        * **4.1 Открой свой "рецепт" (`Dockerfile`) в умном текстовом редакторе** (например, VS Code, Sublime Text, Notepad++).
        * **4.2 Убедись, что файл сохранен с правильными "правилами переноса строк" (LF).** Внизу большинства умных редакторов есть маленькое слово `CRLF` или `LF`. Кликни на него и выбери `LF` (это для Linux-правил), а затем сохрани файл! Это очень частая причина!
        * **4.3 "Перепеки" свой домик:** После того как ты исправишь правила в `Dockerfile`, тебе нужно заново "испечь" шаблон домика, чтобы изменения вступили в силу:
            ```bash
            docker build -t my-ubuntu-ssh-server .
            ```
    * Ты получишь: Твой "домик"-контейнер сможет найти и запустить нужные программы! Ура! 🎉

5.  **`docker-compose command not found` 👻**
    Ты пытаешься позвать на помощь своего супер-стратега Docker Compose, но он молчит! Это может быть, если он не установлен или "потерялся".
    * **Как исправить:**
        * **5.1 Твой Docker Desktop установлен корректно?** 🧐 Новые версии Docker Desktop обычно уже включают Docker Compose. Если что-то пошло не так при установке, попробуй переустановить Docker Desktop.
        * **5.2 Проверь, что Docker Desktop запущен.**
        * **5.3 Если ты на Linux без Docker Desktop:** Возможно, придётся установить Docker Compose plugin отдельно. Погугли официальную инструкцию по установке "Docker Compose plugin for Linux".
    * Ты получишь: Возможность использовать `docker-compose`, и твой супер-стратег будет всегда на связи! 🦸‍♂️

6.  **`ОЙ! yaml: line X: did not find expected key` 😱**
    Вот это классика! Это означает, что твой "план строительства" (`docker-compose.yml`) написан с ошибкой. YAML очень капризный к отступам и к тому, как ты пишешь "ключи" (названия настроек). Он как твой учитель, который придирается к каждой запятой! ✏️
    * **Как исправить:**
        * **6.1 Проверь Отступы!** 📏 YAML использует **пробелы** для отступов, а не табы. Если ты где-то поставил таб вместо пробела, или отступ не совпадает, то Docker Compose сразу ругается. Используй **два или четыре пробела** для каждого уровня вложенности.
            * Открой `docker-compose.yml` в умном текстовом редакторе (VS Code, Sublime Text) – они показывают отступы и помогают их правильно выравнивать.
            * Обрати внимание на строку, которую указал Docker (`line X`). Посмотри, что там написано и какие отступы. Например, если `services:` стоит с неправильным отступом, будет такая ошибка.
            * Убедись, что все ключи (`version`, `services`, `networks`, `build`, `container_name`, `ports`, `environment` и т.д.) правильно выровнены относительно своих "родителей".
        * **6.2 Проверь Заглушки!** 💡 Помни, в `docker-compose.yml` у тебя есть заглушки типа `<ЛОГИН_ПРОКСИ_1>`. Если ты их не заменил на реальные значения, а оставил `<>`, это тоже может вызвать ошибку парсинга YAML. **Убери все угловые скобки `<` и `>` и вставь вместо них реальные данные!** Или, если не знаешь, что туда вставить, закомментируй эти строки символом `#`.
        * **6.3 Опечатки в Ключах!** 📝 Иногда ты можешь просто ошибиться в написании ключа, например, написать `servises` вместо `services`. YAML такой мелочи не прощает.
    * Ты получишь: Аккуратный, правильно написанный `docker-compose.yml`, который Docker Compose поймет с первого раза, и твои сервера запустятся без проблем!

7.  **Почему все контейнеры показывают один и тот же IP? (Исправляем SOCKS5!) 🚨**
    Ты правильно заметил, что все твои контейнеры выходят в интернет с одного и того же IP-адреса. Это значит, что твои SOCKS5-прокси сейчас не работают так, как надо, или настроены некорректно. Вот почему так может быть и что делать:
    * **7.1 Основные Причины:**
        * **Прокси не уникальны или не работают (самая частая причина!).**
            * **ГЛАВНОЕ:** Убедись, что каждый SOCKS5-прокси, который ты указываешь в `docker-compose.yml` (например, для `<ИМЯ_СЕРВИСА_1>` это `<IP_АДРЕС_ПРОКСИ_1>`, для `<ИМЯ_СЕРВИСА_2>` это `<IP_АДРЕС_ПРОКСИ_2>`, для `<ИМЯ_СЕРВИСА_3>` — `<IP_АДРЕС_ПРОКСИ_3>`), на самом деле **является уникальным прокси с уникальным внешним IP-адресом.**
            * Если все твои переменные `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY` для разных контейнеров указывают на один и тот же IP-адрес или домен SOCKS5-сервера, то, конечно, все контейнеры будут выходить в интернет через этот один IP. Тебе нужны **разные SOCKS5-прокси для каждого контейнера**, если ты хочешь разные внешние IP!
            * Убедись, что твои SOCKS5-прокси **работоспособны и доступны** по указанным IP/доменам и портам до того, как ты их используешь в Docker. Попробуй подключиться к каждому прокси отдельно, вне Docker (например, через браузер с настройками прокси, MobaXterm, или специализированные утилиты типа `proxychains`), чтобы убедиться, что они работают и дают нужный внешний IP.
        * **Проблемы с доступом к прокси из контейнера (сеть).**
            * Контейнер может не видеть твой SOCKS5-прокси-сервер. Возможно, файрвол на твоём компьютере или VPS блокирует соединения, или прокси-сервер не слушает на том порту, который ты указал.
            * **Как проверить доступность прокси изнутри контейнера (через SSH):**
                1.  Подключись по SSH к одному из проблемных контейнеров (например, `ssh root@localhost -p 2221`).
                2.  Установи `netcat` (если ещё нет): `apt update && apt install -y netcat-traditional`
                3.  Проверь подключение к прокси-серверу:
                    ```bash
                    nc -vz <IP_АДРЕС_ПРОКСИ_ВАШЕГО_ПРОКСИ> <ПОРТ_ПРОКСИ>
                    # Например: nc -vz myproxy.com 1080
                    ```
                    * **Что ты увидишь**: Если ты видишь `succeeded!`, значит, контейнер может достучаться до прокси. Если нет (`Connection refused` или `No route to host`) — проблема с сетью, файрволом, или сам прокси не запущен/недоступен.
        * **Переменные окружения не подхватились или перезаписались.**
            * Иногда переменные окружения, установленные в `docker-compose.yml`, могут быть перезаписаны скриптами внутри контейнера или неверно интерпретированы.
            * **Как проверить переменные окружения внутри контейнера (через SSH):**
                ```bash
                env | grep -E "PROXY|NO_PROXY"
                ```
                * **Что ты увидишь**: Ты должен увидеть свои прокси-переменные (`HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY`) с правильными значениями. Если их нет или они неправильные, проверь `docker-compose.yml` на опечатки в секции `environment`.
        * **`NO_PROXY` настроен неправильно.**
            * Переменная `NO_PROXY` очень важна. Она говорит контейнеру, куда не нужно ходить через прокси. Если ты случайно указал там IP-адрес твоего **SOCKS5-прокси-сервера**, то контейнер будет его игнорировать и выходить напрямую. Убедись, что в `NO_PROXY` нет IP-адресов твоих SOCKS5-серверов, только внутренние адреса Docker или локальные хосты (как указано в примере).
        * **DNS-разрешение (`socks5` vs `socks5h`).**
            * В переменной `HTTP_PROXY` (и других) ты используешь `socks5://`. По умолчанию `socks5` означает, что DNS-разрешение доменного имени (например, `ifconfig.me`) происходит на стороне контейнера, а уже потом трафик идёт через SOCKS5.
            * Если твой прокси-сервер должен выполнять DNS-разрешение (что часто бывает с SOCKS5-прокси), нужно использовать `socks5h://` (SOCKS5 с разрешением хоста).
            * **Попробуй заменить в `docker-compose.yml`:**
                ```yaml
                socks5://<ЛОГИН_ПРОКСИ_X>:<ПАРОЛЬ_ПРОКСИ_X>@<IP_АДРЕС_ПРОКСИ_X>:1080
                ```
                **на**
                ```yaml
                socks5h://<ЛОГИН_ПРОКСИ_X>:<ПАРОЛЬ_ПРОКСИ_X>@<IP_АДРЕС_ПРОКСИ_X>:1080
                ```
                Это может быть ключевым моментом, если проблема в DNS-запросах.
    * **7.2 Что делать (конкретные шаги для исправления):**
        * **7.2.1 Сначала убедись, что у тебя есть РАЗНЫЕ и РАБОЧИЕ SOCKS5-прокси.** Это 90% успеха! Если у тебя только один прокси, то и IP будет один.
        * **7.2.2 Отредактируй `docker-compose.yml`**:
            * Для каждого сервиса (`<ИМЯ_СЕРВИСА_1>`, `<ИМЯ_СЕРВИСА_2>` и т.д.):
            * **ПЕРЕД перезапуском Docker Compose, внимательно проверь каждую строку `environment`:**
                * **ОБЯЗАТЕЛЬНО** замени все `<ЛОГИН_ПРОКСИ_X>`, `<ПАРОЛЬ_ПРОКСИ_X>`, `<IP_АДРЕС_ПРОКСИ_X>` на **УНИКАЛЬНЫЕ** логины, пароли, IP-адреса/домены и порты для каждого прокси.
                * Попробуй изменить `socks5://` на `socks5h://` для всех твоих `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY` переменных.
        * **7.2.3 Полностью останови и удали старые контейнеры и сеть:**
            * Открой Терминал в папке `мои_тестовые_серверы_docker`.
            ```bash
            docker-compose down # Остановит и удалит все контейнеры и созданную Compose сеть
            ```
            * **Что ты увидишь**: Docker Compose остановит и удалит все контейнеры и сеть.
        * **7.2.4 Пересобери образ** (если менял `Dockerfile`, что в данном случае не нужно, но для надёжности):
            ```bash
            docker build -t my-ubuntu-ssh-server .
            ```
            * **Что ты увидишь**: Docker пересоберет образ.
        * **7.2.5 Запусти контейнеры заново:**
            ```bash
            docker-compose up -d
            ```
            * **Что ты увидишь**: Docker Compose заново запустит все контейнеры и настроит их.
        * **7.2.6 Повторная проверка изнутри контейнера:**
            * Подключись по SSH к каждому из твоих контейнеров.
            * Снова выполни `curl ifconfig.me`.
            * **Что ты увидишь**: Ты должен увидеть уникальные внешние IP-адреса для каждого контейнера.
    * После этих шагов ты сможешь точно понять, где "затык", и заставить каждый твой "домик" выходить в интернет через свой собственный, уникальный IP! Удачи! 💪

---

### Раздел 5: Мы справились! (Заключение) 🎉

Поздравляю, чувак! Ты настоящий викинг мира IT! 🛡️ Ты покорил Docker и освоил Docker Compose! Это реально сложные вещи, а ты с ними справился, как настоящий герой! 💪

Теперь ты умеешь:

* Ставить свой командный центр Docker Desktop. 🚧
* Запускать свои изолированные "домики"-контейнеры Ubuntu 22.04. 🏡
* Настраивать сети, чтобы у каждого "домика" был свой чёткий адрес. 🌐
* Юзать Docker Compose для управления целыми "фермами" одинаковых тестовых серверов! 🚜
* Давать каждому серверу свой уникальный секретный туннель (SOCKS5-прокси) для выхода в интернет. 🕵️‍♀️
* Подключаться к каждому серверу через SSH-телефон! 🔑

Терминал (это чёрное окошко)? Раньше, может, и бесил, а теперь он твой лучший друг и инструмент! 🤝

💡 **Реальный совет (на вес золота):** 💰 Никогда, **НИКОГДА** не сдавайся, если что-то не получается! 🧘 Docker — это как очень сложный конструктор: иногда детальки не подходят, и всё "рушится" 💔, но потом ты врубаешься и строишь целый космический корабль! 🚀 Ищи ответы в доках 📚, на Stack Overflow 🤓, спрашивай у знающих 🗣️. Каждая "ой!"-ошибка — это твой личный тренажёр, который делает тебя сильнее! 💪

---

### Раздел 6: Маленькие хитрости для Профи 🌟

Если ты хочешь стать ещё более крутым чуваком в Docker, вот тебе несколько дополнительных фишек:

1.  **Как посмотреть и убрать старые игрушки (Docker-образы) 🧹**
    Твои "шаблоны домиков" (Docker-образы) могут занимать место. Вот как их чекнуть и почистить, когда они больше не нужны:
    * **1.1 Посмотреть все свои шаблоны:**
        Чтобы увидеть список всех "шаблонов домиков", которые у тебя есть на компе, используй:
        ```bash
        docker images
        # или
        docker image ls
        ```
        * **Что ты увидишь**: Табличку с колонками: `REPOSITORY`, `TAG`, `IMAGE ID`, `CREATED`, `SIZE`. Твой `my-ubuntu-ssh-server` должен быть там.
    * **1.2 Убрать старый шаблон:**
        Чтобы убрать шаблон, нужен его `IMAGE ID` (это уникальный "номер") или `REPOSITORY` (имя) и `TAG` (версия).
        ```bash
        docker rmi <IMAGE_ID>
        # или
        docker rmi <ИМЯ_ТВОЕГО_ОБРАЗА>:<TAG>
        ```
        Примеры:
        * Убрать `my-ubuntu-ssh-server:latest` по имени:
            ```bash
            docker rmi my-ubuntu-ssh-server:latest
            ```
        * Убрать шаблон по его "номеру" (вот так выглядит `IMAGE ID`):
            ```bash
            docker rmi d38600b308fc # (замени на настоящий "номер" из команды docker images!)
            ```
        **ВАЖНО:** Нельзя убрать шаблон, если от него сейчас "живут" контейнеры! Сначала надо их "уложить спать" и "убрать за собой". 😴
    * **1.3 Чтобы "уложить спать" все запущенные домики (остановить):**
        ```bash
        docker stop $(docker ps -aq)
        ```
        * **Что ты увидишь**: Сообщение об остановке контейнеров.
    * **1.4 Чтобы "убрать" все "спящие" домики (удалить):**
        ```bash
        docker rm $(docker ps -aq)
        ```
        * **Что ты увидишь**: Сообщение об удалении контейнеров.
    * Только после этого ты сможешь удалить шаблон!

2.  **Используй большой "магазин игрушек" (Docker Hub) 🎁**
    `docker pull [имя_образа]`
    * **Что это?** Это как зайти в огромный онлайн-магазин, где есть тысячи уже готовых "домиков"-образов для всего на свете!
    * Ты получишь: Экономию времени и тысячи проверенных решений, сделанных другими крутыми чуваками! ✅

3.  **Храни свои "рецепты" и "планы" в "коробке сокровищ" (Git) 🗝️**
    Это система контроля версий, как супер-история твоих изменений.
    * **Что это?** Git — это как волшебная коробка, которая запоминает все-все изменения в твоих файлах (`Dockerfile`, `docker-compose.yml`, `start.sh`).
    * Они спасут твой проект, если что-то потеряешь, и помогут работать с командой (если ты станешь тимлидом, конечно! 😉).
    * Ты получишь: Безопасность настроек и отслеживание изменений.

4.  **Придумывай реально угарные имена для своих домиков-контейнеров! 🤣**
    Это поднимет тебе настроение и поможет легко их различать! Вместо `container_123` назови `spiderman_server` или `super_secret_base`!
    * Ты получишь: Приятную работу и лёгкую идентификацию.

5.  **Используй красивое окошко Docker Desktop (GUI) 🖼️**
    Это как карта твоего города Docker, где ты можешь видеть все свои домики, что они "говорят" (логи) и сколько "сил" (ресурсов) они тратят. Удобная штука!
    * Ты получишь: Визуальный контроль и удобство.

6.  **Изучи `.dockerignore` 🙈**
    * **Что это?** Это как список "что не брать с собой" при сборке "домика". Он игнорирует ненужные файлы и папки из твоего проекта, чтобы твои шаблоны были маленькими и быстро "пеклись"!
    * Ты получишь: Меньшие по размеру и быстрые в сборке образы.

🐾 P.S. Если после всего этого всё равно что-то тупит — просто напиши мне! Мы тут все учимся, и вместе мы обязательно разберёмся! 💪 Мы банда! 🤝
