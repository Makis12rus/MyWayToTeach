# 🧱 Основные зависимости проекта: изоляция, версии и спокойная жизнь

> Гайд для тех, кто не хочет «чинить интернет» каждый раз, когда запускает проект на новом ноутбуке.

---

## ✅ Короткий чек‑лист перед стартом

1. Определяем цель: изолировать окружение, зафиксировать версии, добиться повторяемости установки.
2. Выбираем стек инструментов (базовый, современный быстрый, «всё‑в‑одном»). Не мешаем инструменты между собой.
3. Создаём виртуальное окружение в папке проекта и обновляем `pip`.
4. Описываем зависимости декларативно и фиксируем их в lock или в `requirements.txt`.
5. Синхронизируем окружение строго по фиксациям и проверяем импорт/версию.
6. Документируем команду восстановления окружения для коллег и CI.

---

## 📝 Примечания

* В этом гайде все инструменты устанавливаются и запускаются **только внутри виртуального окружения проекта** (папка `.venv`). Это чуть менее магично, зато предсказуемо.
* В примерах указаны версии вроде `3.12` и `2.32.3`. У вас они могут отличаться — выбирайте актуальные для вашего проекта.
* Выберите **один** менеджер зависимостей на проект: `pip/pip-tools`, `uv`, `Poetry`, `PDM`, `Rye` или `Pipenv`. Смешивание увеличивает шанс конфликтов.

---

## 🧭 Содержание

* [Введение: зачем всё это](#введение-зачем-всё-это)
* [Выбор стека (коротко)](#выбор-стека-коротко)
* [venv](#venv) — стандартный способ создать изолированное окружение
* [pip](#pip) — базовая установка пакетов
* [uv](#uv) — быстрые установки и lock
* [pip-tools](#pip-tools) — железобетонные `requirements.txt`
* [virtualenv](#virtualenv) — гибкое создание окружений
* [Poetry](#poetry) — управление через `pyproject.toml` и `poetry.lock`
* [PDM](#pdm) — современный менеджер на PEP‑рельсах
* [Rye](#rye) — версии Python + зависимости + окружение
* [Pipenv](#pipenv) — `Pipfile` и автогенерация окружения
* [Итоговое резюме и рекомендуемые стеки](#итоговое-резюме-и-рекомендуемые-стеки)
* [Рекомендации и полезные привычки](#рекомендации-и-полезные-привычки)

---

## Введение: зачем всё это

Представьте, что ваш проект — это аквариум. Вода — версии пакетов. Если каждый раз наливать «что было под рукой», рыбкам недолго осталось. Мы строим **свой аквариум** (виртуальное окружение), **фиксируем состав воды** (lock/requirements) и получаем **одинаковый результат** на любой машине.

---

## Выбор стека (коротко)

* **Базовый и понятный:** `venv` + `pip` + `pip-tools` — классика для большинства проектов.
* **Сверхбыстрый и компактный:** `uv` вместо `pip`/`pip-tools` (умеет lock и sync, плюс управление Python).
* **Проектный менеджмент из коробки:** `Poetry` или `PDM` — зависимости в `pyproject.toml`, lock и запуск команд.
* **Всё‑в‑одном:** `Rye` — фиксирует Python, зависимости и окружение.
* **Совместимость со старыми пайплайнами:** `Pipenv` — используйте там, где он уже принят.

> Далее — короткие, практичные блоки по каждому инструменту. Везде принцип один: **сначала окружение**, потом **фиксации**, затем **sync** и **проверка**.

---

## venv

Создаёт отдельную папку с собственным Python и пакетами, чтобы не портить систему.

```bash
python -m venv .venv
```

Создаёт папку `.venv` с интерпретатором и каталогами для пакетов.

```bash
# macOS/Linux
source .venv/bin/activate
# Windows PowerShell
.\.venv\Scripts\Activate.ps1
```

Активирует окружение. В приглашении появится префикс `(.venv)`.

```bash
python -m pip install --upgrade pip
```

Обновляет менеджер пакетов внутри окружения, чтобы меньше ловить ошибки.

**Проверка:**

```bash
python -c "import sys; print(sys.executable)"
```

Ожидаем путь, оканчивающийся на `.../.venv/bin/python` или `.../.venv/Scripts/python.exe`.

---

## pip

Базовый установщик пакетов. Работает «где активирован, туда и ставит».

```bash
python -m pip install requests
```

Ставит пакет в активированное окружение.

```bash
python -m pip freeze > requirements.txt
```

Записывает точные версии в файл фиксаций.

```bash
python -m pip install --require-hashes -r requirements.txt
```

Строгая установка по фиксациям и хешам для детерминизма и безопасности.

**Проверка:**

```bash
python -c "import requests; print(requests.__version__)"
```

Должна напечататься установленная версия.

---

## uv

Сверхбыстрый инструмент: умеет `pip`‑режим, компиляцию требований и `sync`, а ещё управляет версиями Python.

```bash
uv venv .venv && source .venv/bin/activate
```

Создаёт окружение и активирует его.

```bash
uv pip install "requests==2.32.3"
```

Ставит точную версию пакета быстро и в текущее окружение.

```bash
uv pip compile requirements.in -o requirements.txt && uv pip sync requirements.txt
```

Генерирует зафиксированные версии и приводит окружение строго к ним.

**Альтернатива через проект:**

```bash
uv init
uv add requests==2.32.3
uv sync --frozen
```

Создаёт `pyproject.toml`, фиксирует зависимости в lock и устанавливает их строго по lock.

**Проверка:**

```bash
python -c "import requests, sys; print('OK', requests.__version__, sys.executable)"
```

Ожидаем `OK` и путь к `.venv`.

---

## pip-tools

Добавляет «бетон»: из чистого списка желаний делает точные фиксации и синхронизирует окружение.

```bash
python -m pip install pip-tools
```

Устанавливает `pip-compile` и `pip-sync` в активированное окружение.

```text
# requirements.in — пишем только верхнеуровневые зависимости
requests==2.32.3
uvicorn>=0.30
```

Декларативный список желаемых пакетов.

```bash
pip-compile requirements.in -o requirements.txt
pip-sync requirements.txt
```

Генерирует точные версии и приводит окружение к ним.

**Проверка:**

```bash
python -m pip check
```

Сообщит о возможных конфликтах версий.

---

## virtualenv

Гибкий и быстрый конструктор окружений, особенно когда нужен конкретный Python.

```bash
virtualenv -p python3.12 .venv312
```

Создаёт окружение, жёстко привязанное к Python 3.12.

```bash
source .venv312/bin/activate && python -m pip install --upgrade pip
```

Активируем и обновляем базовые инструменты.

```bash
python -m pip install requests && python -c "import requests; print(requests.__version__)"
```

Ставим пакет и сразу проверяем, что он доступен в этом окружении.

---

## Poetry

Менеджер, который хранит зависимости в `pyproject.toml` и фиксирует их в `poetry.lock`.

```bash
python -m pip install poetry
poetry config virtualenvs.in-project true
poetry init -n
poetry add requests==2.32.3
poetry install --sync
poetry run python -c "import requests; print(requests.__version__)"
```

Создаём проект, включаем хранение `.venv` в папке проекта, добавляем зависимость, ставим строго по lock и проверяем импорт в контексте окружения.

---

## PDM

Современный менеджер: зависимости и lock, запуск команд через `pdm run`.

```bash
python -m pip install pdm
pdm init -n
pdm add requests==2.32.3
pdm lock && pdm install
pdm run python -c "import requests; print(requests.__version__)"
```

Минимум кликов — максимум воспроизводимости.

---

## Rye

Комбайн: фиксирует версию Python, зависимости и окружение.

```bash
python -m pip install rye
rye init
rye pin 3.12
rye add requests==2.32.3
rye sync --frozen
rye run python -c "import requests; print(requests.__version__)"
```

Один набор команд — и у всей команды одинаковые Python и пакеты.

---

## Pipenv

Исторически популярный вариант с `Pipfile` и `Pipfile.lock`.

```bash
python -m pip install pipenv
set PIPENV_VENV_IN_PROJECT=1   # Windows
# или export PIPENV_VENV_IN_PROJECT=1 для macOS/Linux
pipenv --python 3.12
pipenv add requests==2.32.3
pipenv sync --deploy
pipenv run python -c "import requests; print(requests.__version__)"
```

Создаём проект, храним `.venv` в корне, фиксируем версии и запускаем команды в нужном окружении.

---

## Итоговое резюме и рекомендуемые стеки

* **Базовый надёжный стек (рекомендуется по умолчанию):** `venv` + `pip` + `pip-tools`
  Причина: простота, прозрачные `requirements.txt`, предсказуемая установка на любой машине.

* **Современный быстрый стек:** `uv`
  Причина: скорость, lock и sync из коробки, единый инструмент для проекта.

* **Проектный менеджмент:** `Poetry` **или** `PDM`
  Причина: хранение зависимостей в `pyproject.toml`, удобные команды, детерминированные lock‑файлы.

* **Всё‑в‑одном:** `Rye`
  Причина: фиксирует и Python, и зависимости, и окружение; удобно для командной разработки.

* **Совместимость/наследие:** `Pipenv`
  Причина: используйте там, где он исторически принят или требуется инфраструктурой.

* **Тонкая настройка окружений:** `virtualenv`
  Причина: когда нужен специфический интерпретатор или параметры создания окружения.

**Золотое правило:** один проект — один менеджер. Смешивание инструментов увеличивает хаос и уменьшает счастье.

---

## Рекомендации и полезные привычки

* Держите окружение **внутри проекта** (`.venv`) и добавьте его в `.gitignore`.
* Всегда фиксируйте версии: `pip-compile`/`uv lock`/`poetry lock`/`pdm lock`/`Pipfile.lock`/`rye lock`.
* Для CI используйте строгие режимы: `--require-hashes`, `--frozen`, `--sync`, `--deploy`.
* Храните «верхнеуровневые» зависимости отдельно (`requirements.in` или группы зависимостей в `pyproject.toml`).
* Документируйте быстрый запуск проекта в `README`: 3–5 команд для «клонировал и побежал».

---

### Конец

Теперь у вас есть не просто инструкция, а инструкция с характером. Создавайте окружение, фиксируйте версии и живите спокойно: код будет работать одинаково и у вас, и у вашей команды, и у таинственного сервера в продакшене. Ура.
