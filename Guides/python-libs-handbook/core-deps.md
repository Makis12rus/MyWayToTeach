# 🧱 Основные зависимости проекта: как подружить ваш код с любой машиной

> Представьте, что проект — это оркестр. У каждого своя партия, но звучит стройно только тогда, когда дирижёр следит за темпом и составом. В роли дирижёра — вы, виртуальное окружение и фиксации версий. Этот каркас — о том, как выстроить повествование так, чтобы главные актёры (зависимости) вышли **в середине** спектакля и удержали внимание.

---

## Примечания

* Во всём тексте предполагается работа **внутри виртуального окружения проекта** (папка `.venv`). Так пакеты не смешиваются с системными и чужими проектами.
* Версии Python и библиотек в примерах (`3.12`, `2.32.3`) — ориентиры. Берите те, что согласованы внутри команды.
* На один проект выбирайте **один** менеджер зависимостей: `pip/pip-tools`, `uv`, `Poetry`, `PDM`, `Rye` или `Pipenv`.
* Ниже приведён **каркас**: все разделы на своих местах; подробные блоки по инструментам будут добавлены после раздела «Как устроена магия: простыми словами», то есть **в середине** гайда.

---

## Содержание 🧭

1. [Введение: зачем вам изоляция и фиксации](#введение-зачем-вам-изоляция-и-фиксации)
2. [Как устроена магия: простыми словами](#как-устроена-магия-простыми-словами)
3. **Зависимости**:

   * [venv](#venv) — стандартный модуль для создания виртуальных окружений в папке проекта.
   * [pip](#pip) — базовый установщик пакетов; ставит зависимости в активированное окружение.
   * [uv](#uv) — сверхбыстрый менеджер пакетов и окружений; lock и строгий sync из коробки.
   * [pip-tools](#pip-tools) — компиляция фиксированных `requirements.txt` и синхронизация окружения.
   * [virtualenv](#virtualenv) — гибкое и быстрое создание окружений с выбором версии Python.
   * [Poetry](#poetry) — управление зависимостями в `pyproject.toml` и фиксации в `poetry.lock`.
   * [PDM](#pdm) — современный менеджер по актуальным PEP; `pyproject.toml` + `pdm.lock` + `pdm run`.
   * [Rye](#rye) — единый инструмент: версии Python, зависимости и виртуальное окружение с `sync` по lock.
   * [Pipenv](#pipenv) — `Pipfile`/`Pipfile.lock` и автоматическое создание окружения `.venv` в проекте.
4. [Выбор стека: какой путь подойдёт именно вам](#выбор-стека-какой-путь-подойдёт-именно-вам)
5. [Типовой жизненный цикл проекта](#типовой-жизненный-цикл-проекта)
6. [Структура проекта и ключевые файлы](#структура-проекта-и-ключевые-файлы)
7. [Быстрый старт: минимальные сценарии](#быстрый-старт-минимальные-сценарии)
8. [Диагностика и частые ошибки](#диагностика-и-частые-ошибки)
9. [Безопасность и доверие пакетам](#безопасность-и-доверие-пакетам)
10. [CI/CD: чтобы сервер не капризничал](#cicd-чтобы-сервер-не-капризничал)
11. [Итоговое резюме: рекомендуемые стеки](#итоговое-резюме-рекомендуемые-стеки)
12. [Рекомендации и полезные привычки](#рекомендации-и-полезные-привычки)


---

## Введение: зачем вам изоляция и фиксации

Код может быть прекрасным, но если он полагается на «какие попались версии пакетов», приключения обеспечены. Решение состоит из трёх простых идей:

1. **Изоляция** — у проекта свой мини-мир, виртуальное окружение. Пакеты не конфликтуют с системой и другими проектами.
2. **Фиксации** — записываем точные версии в особые файлы (lock или `requirements.txt`). Сегодня и завтра получаем одно и то же.
3. **Синхронизация** — на любой машине восстанавливаем окружение **строго по фиксациям**.

Результат: воспроизводимость и спокойные релизы.

---

## Как устроена магия: простыми словами

* **Виртуальное окружение** — как отдельная кухня в общей квартире: своя посуда, свои специи, соседи не лезут. Активировали — готовите только у себя.
* **`requirements.txt`** — список покупок с точными марками. Если списки совпадают, блюда получаются одинаково.
* **Lock‑файл** — строгий рецепт всего «лесенка зависимостей», а не только верхних пакетов. Идеален для командной работы.
* **Менеджер зависимостей** — ваш «закупщик». Он знает, где что взять и как вернуть, если не подошло.

---

# Блоки по инструментам (подробные разделы начинаются здесь)

> Ниже расположены заголовки разделов для каждого инструмента. Содержимое будет добавляться по мере подготовки. Разделы стоят **в середине** гайда, как вы и требовали, чтобы логика повествования оставалась живой и последовательной.

## venv

**venv** — стандартный модуль Python, который создаёт **виртуальное окружение** прямо в папке проекта. Это отдельная «песочница» со своим интерпретатором и собственными пакетами: ничего не попадает в систему и не конфликтует с другими проектами. Идея простая: всё, что ставите, живёт в `.venv`, а не разгуливает по всему компьютеру.

> Важно: все команды ниже выполняйте **в корне вашего проекта**. Папку `.venv/` добавьте в `.gitignore`, чтобы не коммитить её в репозиторий.

---

### 🔎 Шаг 0: Проверяем, что Python установлен

```bash
python --version   # или python3 --version
```

На Windows может работать лаунчер:

```bat
py -V
```

**Что это даёт?** Убеждаемся, что Python доступен из терминала и видим его версию (например, `Python 3.12.x`). Если команды не находятся, установите Python и перезапустите терминал.

---

### 🏗️ Шаг 1: Создаём виртуальное окружение в проекте

```bash
python -m venv .venv
```

**Что происходит?** Запускается модуль `venv`, который создаёт папку `.venv` со своим интерпретатором и каталогом для пакетов. Точка в начале делает папку скрытой на Unix и просто аккуратно прячет служебные файлы.

Полезные варианты (не обязательно, но удобно):

```bash
python -m venv .venv --prompt "proj"   # красивый префикс в приглашении
python -m venv .venv --upgrade-deps     # сразу обновить базовые инструменты в окружении
```

**Ожидаемый результат:** в проекте появилась папка `.venv/` с подкаталогами `bin/` (или `Scripts/` на Windows), `Lib/` и служебными файлами.

---

### 🔑 Шаг 2: Активируем окружение

**macOS/Linux (bash/zsh):**

```bash
source .venv/bin/activate
```

**Windows (PowerShell):**

```powershell
.\.venv\Scripts\Activate.ps1
```

**Windows (cmd.exe):**

```bat
.\.venv\Scripts\activate.bat
```

**Что это даёт?** Терминал «переключается» на Python из `.venv`. В приглашении появится префикс `(.venv)` или тот, что вы указали через `--prompt`.

Проверка активации:

```bash
echo $VIRTUAL_ENV        # macOS/Linux
```

```powershell
$env:VIRTUAL_ENV         # Windows PowerShell
```

Ожидаем путь к `.../project/.venv`.

Чтобы выключить окружение в любом терминале:

```bash
deactivate
```

> На Windows PowerShell иногда мешает политика выполнения скриптов. Если видите предупреждение — проще запускать команды через `python -m` (см. ниже) или временно открыть PowerShell «Запуск от имени администратора» и следовать подсказке системы.

---

### 🧪 Шаг 3: Проверяем, что используем «тот самый» Python

```bash
python -c "import sys; print(sys.executable)"
```

Ожидаем путь к интерпретатору **из `.venv`**, например:

```text
/path/to/project/.venv/bin/python
```

Если путь указывает на системный Python, окружение не активировано.

---

### 🛠️ Шаг 4: Обновляем менеджер пакетов внутри окружения

```bash
python -m pip install --upgrade pip
```

**Зачем?** Свежий `pip` быстрее собирает колёса и реже спотыкается на установках. Флаг `-m` гарантирует запуск `pip` именно у активного интерпретатора.

(Опционально) Обновим вспомогательные инструменты сборки:

```bash
python -m pip install --upgrade setuptools wheel
```

**Когда это нужно?** Если проект собирает локальные колёса или ставит пакеты из исходников.

---

### 📦 Шаг 5: Ставим пакеты **только** внутрь `.venv` и проверяем

Пример установки популярной библиотеки:

```bash
python -m pip install requests
```

**Что делает команда?** Устанавливает `requests` **внутрь активированного окружения**. Никаких следов в системе.

Проверим, что пакет действительно в окружении, а не где-то ещё:

```bash
python -c "import requests, sys; print('OK', requests.__version__, sys.executable)"
```

Ожидаем примерно такое:

```text
OK 2.32.3 /path/to/project/.venv/bin/python
```

Здесь

* `OK` — импорт успешен,
* `2.32.3` — версия установленного пакета,
* путь указывает на Python из `.venv`.

---

### 🧾 Шаг 6: Фиксируем текущее состояние окружения (для воспроизведения)

```bash
python -m pip freeze > requirements.txt
```

**Что это?** «Снимок» точных версий всех пакетов, установленных в `.venv`. Этот файл кладут в репозиторий, чтобы коллеги и CI получили **те же** версии.

Восстановление на другой машине:

```bash
python -m venv .venv
source .venv/bin/activate            # Windows: .\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

**Результат:** окружение полностью совпадает по версиям.

> Если нужна **ещё более строгая** установка, генерируйте `requirements.txt` с хешами (см. инструмент `pip-tools`) и ставьте через `--require-hashes`.

---

### 🧹 Шаг 7: Как безопасно пересоздать окружение

Иногда проще начать с чистого листа (например, перед большим обновлением зависимостей).

**macOS/Linux:**

```bash
rm -rf .venv
python -m venv .venv
source .venv/bin/activate
```

**Windows (PowerShell):**

```powershell
Remove-Item -Recurse -Force .venv
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

После пересоздания установите зависимости из `requirements.txt`, как показано выше.

---

### 🧯 Шаг 8: Диагностика частых проблем

* **«Команда не найдена / пакет не импортируется».** Почти всегда окружение не активировано. Проверьте `sys.executable` и переменную `VIRTUAL_ENV`.
* **«Ставится не туда».** Используйте форму `python -m pip ...`, чтобы точно привязать `pip` к активному интерпретатору.
* **«Версия пакета не та».** Вы забыли зафиксировать версии. Сделайте `pip freeze > requirements.txt` и используйте этот файл на всех машинах.
* **«Нужно несколько версий Python».** Создайте несколько окружений: `.venv311`, `.venv312` и активируйте нужное в зависимости от задачи.

---

### 🗂️ Что в итоге должно быть в проекте

```text
project/
├─ .venv/                 # изолированное окружение проекта
├─ requirements.txt       # снимок версий (если используете freeze)
├─ src/ or your_code.py   # ваш код
└─ README.md              # инструкции для команды
```

**Итог:** у проекта есть свой карманный Python и свои пакеты; все установки проходят внутрь `.venv`, а воспроизведение окружения занимает несколько команд. Именно это и даёт предсказуемый запуск кода на любой машине.


## pip

*Здесь позже появится полный раздел с пошаговыми командами, объяснениями и ожидаемыми результатами.*

## uv

*Здесь позже появится полный раздел с пошаговыми командами, объяснениями и ожидаемыми результатами.*

## pip-tools

*Здесь позже появится полный раздел с пошаговыми командами, объяснениями и ожидаемыми результатами.*

## virtualenv

*Здесь позже появится полный раздел с пошаговыми командами, объяснениями и ожидаемыми результатами.*

## Poetry

*Здесь позже появится полный раздел с пошаговыми командами, объяснениями и ожидаемыми результатами.*

## PDM

*Здесь позже появится полный раздел с пошаговыми командами, объяснениями и ожидаемыми результатами.*

## Rye

*Здесь позже появится полный раздел с пошаговыми командами, объяснениями и ожидаемыми результатами.*

## Pipenv

*Здесь позже появится полный раздел с пошаговыми командами, объяснениями и ожидаемыми результатами.*

---

## Выбор стека: какой путь подойдёт именно вам

* **Простой и понятный:** `venv` + `pip` + `pip-tools` — минимум новой магии, максимум контроля.
* **Современный быстрый:** `uv` — один инструмент для окружений, фиксаций и установки.
* **«Проектный» подход:** `Poetry` или `PDM` — зависимости в `pyproject.toml`, lock, группы и удобный `run`.
* **Всё‑в‑одном:** `Rye` — фиксирует Python и пакеты, синхронизирует окружение в одну кнопку.
* **Наследие/совместимость:** `Pipenv` — используйте там, где он уже принят.

---

## Типовой жизненный цикл проекта

1. Создаёте репозиторий и `.gitignore`. Поднимаете `.venv` в корне.
2. Описываете зависимости: либо `requirements.in`/`requirements.txt`, либо `pyproject.toml` + lock.
3. Устанавливаете строго по фиксациям. Если что-то меняете — пересобираете фиксации.
4. Проверяете импорт ключевых пакетов и путь к интерпретатору (`sys.executable` должен указывать в `.venv`).
5. Коллеги клонируют репозиторий и повторяют те же команды, получая идентичное окружение.
6. Обновляете версии осознанно — массово или точечно — и фиксируете причину в истории изменений.

---

## Структура проекта и ключевые файлы

```text
project/
├─ .venv/              # виртуальное окружение проекта (не коммитится)
├─ src/ or your_code.py
├─ tests/
├─ requirements.in     # верхнеуровневые зависимости (если используется)
├─ requirements.txt    # зафиксированные версии (если используется)
├─ pyproject.toml      # конфигурация и зависимости (если используется)
├─ poetry.lock | pdm.lock | uv.lock | Pipfile.lock | requirements.lock
├─ Pipfile             # если используете Pipenv
├─ .gitignore
└─ README.md
```

---

## Быстрый старт: минимальные сценарии

**Классика (venv + pip + pip-tools)**

```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip pip-tools
pip-compile requirements.in -o requirements.txt
pip-sync requirements.txt
```

**Современный быстрый (uv)**

```bash
uv venv .venv && source .venv/bin/activate
uv pip install -r requirements.txt   # или uv add ...; uv sync --frozen
```

---

## Диагностика и частые ошибки

* Пакет «не виден» после установки — почти всегда забыто активировать `.venv`. Проверьте `sys.executable`.
* Конфликт версий — пересоберите фиксации (`pip-compile`/`poetry lock`/`pdm lock`/`uv ...`/`rye lock`) и установите по ним.
* Windows ругается на скрипты — используйте `poetry run`/`pdm run`/`pipenv run`/`rye run`, чтобы не воевать с политиками.
* Смешали инструменты — создайте окружение заново и ведите проект одним менеджером.

---

## Безопасность и доверие пакетам

* Включайте строгие режимы: `--require-hashes`, `--frozen`, `--sync`, `--deploy` — по возможностям выбранного инструмента.
* Коммитьте lock‑файлы и ревьюйте их так же строго, как код.
* Используйте официальные индексы или доверенные зеркала.

---

## CI/CD: чтобы сервер не капризничал

* Закрепляйте версию Python локально и на CI.
* Повторяйте команды синхронизации: `pip-sync`, `uv sync --frozen`, `poetry install --sync`, `pdm install`, `rye sync --frozen`, `pipenv sync --deploy`.
* Кэшируйте зависимости, не смешивая кэши разных версий Python.

---

## Итоговое резюме: рекомендуемые стеки

* **По умолчанию:** `venv` + `pip` + `pip-tools` — просто, прозрачно, переносимо.
* **Быстро и компактно:** `uv` — один инструмент для всего цикла.
* **Проектный менеджмент:** `Poetry` **или** `PDM` — lock‑файл, группы, удобный `run`.
* **Всё‑в‑одном:** `Rye` — фиксирует Python и пакеты, строгий `sync`.
* **Совместимость с историей:** `Pipenv` — используйте там, где он уже принят.

---

## Рекомендации и полезные привычки

* Держите `.venv/` в проекте и в `.gitignore`.
* Фиксируйте версии и обновляйте их осознанно, с записью причин.
* Документируйте «быстрый старт» в `README` (3–5 команд).
* При крупных апдейтах быстрее пересоздать окружение, чем охотиться за «призраками» зависимостей.
