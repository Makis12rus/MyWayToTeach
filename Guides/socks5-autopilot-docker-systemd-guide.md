# Гайд для ленивых гениев: Устанавливаем SOCKS5-сервер, который сам встаёт с дивана

Привет, будущий повелитель интернета! 🎩✨ Готовься к погружению в мир SOCKS5-прокси, где твои данные будут скользить по сети, как ниндзя в ночи. Мы не просто установим сервер, мы сделаем это стильно, с юмором и так, чтобы он сам вставал после перезагрузки VPS, как по щелчку пальца! 💪

Забудь о рутине, мы сделаем твой SOCKS5-сервер *независимым* и *самостоятельным*, словно бунтарь-подросток, но при этом *ответственным* и *исполнительным*. 😄

---

### Раздел 1: Предварительная настройка сервера — Чтобы всё работало как по маслу! 🛠️

Прежде чем мы начнем устанавливать что-то серьёзное, убедись, что твой VPS-сервер в отличной форме! Это как проверить масло в машине перед долгой поездкой — мелочь, но очень важная. Все эти команды выполняй прямо **в корневой директории твоего пользователя** после подключения по SSH (обычно это `~` или `/home/твоё_имя_пользователя`).

1.  **Обнови систему**: Убедись, что все пакеты на твоём сервере свежие и блестящие. Это предотвратит всякие неприятные сюрпризы и обеспечит совместимость.

    ```bash
    sudo apt update && sudo apt upgrade -y
    ```
    Это как генеральная уборка в доме перед приходом гостей! 🧹✨ Не забудь про печеньки! 🍪

    * **Что ты увидишь**: После `sudo apt update` будет длинный список обновляемых пакетов и их репозиториев. Затем `sudo apt upgrade -y` покажет процесс установки обновлений, после чего появится сообщение о завершении, например: `0 upgraded, 0 newly installed, 0 to remove and 0 not upgraded.` (если всё уже обновлено) или список устанавливаемых пакетов. ✅

2.  **Установи необходимые инструменты**: Обычно они уже есть, но мало ли! `nano` (или твой любимый текстовый редактор) и `curl` или `wget` для скачивания чего-либо могут пригодиться.

    ```bash
    sudo apt install -y nano curl wget
    ```
    Готовим наш набор юного хакера! 🤓 А вдруг ты захочешь что-то покреативнее `nano`? 🤔

    * **Что ты увидишь**: Система покажет процесс установки, а затем сообщение о том, что пакеты установлены, например: `0 upgraded, 0 newly installed, 0 to remove and 0 not upgraded.` (если уже установлены) или `Setting up nano (version)...` и т.д. ✅

---

### Раздел 2: Подготовка к установке — Магия начинается! 🪄

Теперь, когда сервер готов к работе, убедись, что у тебя есть:

1.  ***VPS-сервер***: Твой личный цифровой особняк, где мы будем творить чудеса. 🏰👑
2.  ***SSH-доступ***: Ключи от этого особняка, позволяющие тебе управлять им удалённо. 🔑 Никому их не показывай! 🤫
3.  ***Установленный Docker***: Наш маленький, но могучий помощник, который упакует сервер в удобный, изолированный контейнер. Если его ещё нет, бегом устанавливать! 🏃‍♂️💨 Эти команды также выполняй **в любой директории**, например, в домашней:

    ```bash
    sudo apt install docker.io -y
    sudo systemctl enable docker
    sudo systemctl start docker
    ```
    Эти команды устанавливают Docker, затем включают его автоматический запуск при старте системы (`enable`) и запускают его прямо сейчас (`start`). Это как дать Докеру разрешение жить на твоём сервере и убедиться, что он просыпается вместе с ним. 🚀 Пусть трудится! 💪

    * **Что ты увидишь**:
        * После `sudo apt install docker.io -y`: Информацию об установке пакетов Docker. ✅
        * После `sudo systemctl enable docker`: Обычно ничего не выводит, если команда прошла успешно. ✅
        * После `sudo systemctl start docker`: Тоже ничего не выводит, если Docker запустился без ошибок. ✅

---

### Раздел 3: Установка SOCKS5-сервера — Шаг за шагом к свободе! 🚀

#### Шаг 3.1: Свой Уголок для SOCKS5 — Домик для Прокси 🏡

Представь, что твой SOCKS5-сервер — это маленький, но очень важный гость. Ему нужен свой личный уголок, чтобы не мешать другим жильцам (твоим скриптам и приложениям) и чувствовать себя комфортно. Создадим для него такую "комнату", где будут храниться все его файлы. Выполняй эти команды **в домашней директории** (или любой другой, где хочешь создать папку для прокси):

```bash
mkdir -p ~/my_socks5_server
cd ~/my_socks5_server
```
* `mkdir -p ~/my_socks5_server`: Создаёт директорию `my_socks5_server` в твоей домашней папке (`~`). Опция `-p` гарантирует, что если родительские директории не существуют, они тоже будут созданы.
* `cd ~/my_socks5_server`: Переходит в только что созданную директорию.

Теперь мы находимся *внутри* этого уютного каталога. Все наши дальнейшие действия будут происходить здесь, чтобы ничто не пересекалось! 🧹📁 Наводим порядок! ✨

* **Что ты увидишь**: Обычно эти команды не выводят никакого текста, если они выполнены успешно. Просто вернется приглашение командной строки, которое будет показывать твое текущее местоположение, например: `user@your-vps-name:~/my_socks5_server$`. Это значит, что директория создана и ты в нее перешел. ✅

#### Шаг 3.2: Загрузка "Багажа" — Docker-образ на Борту! 🚢

Теперь пришло время привезти "багаж" для нашего гостя — Docker-образ SOCKS5-сервера. Это как готовый домик на колёсах, который мы просто "скачаем" и "развернём" из публичного репозитория Docker Hub. Эту команду можно выполнять **в любой директории**, но лучше всего оставаться **в `~/my_socks5_server`**, чтобы всё было в одном месте:

```bash
docker pull dijedodol/simple-socks5-server
```

Эта команда скачивает официальный образ `dijedodol/simple-socks5-server` с Docker Hub. Пара секунд, и образ скачан! Теперь у тебя есть всё необходимое, чтобы запустить сервер. Легко, правда? 😎📦 Почти как доставка пиццы, только быстрее! 🍕💨

* **Что ты увидишь**: Процесс скачивания слоёв Docker-образа, например:
    ```
    Using default tag: latest
    latest: Pulling from dijedodol/simple-socks5-server
    Digest: sha256:...
    Status: Downloaded newer image for dijedodol/simple-socks5-server:latest
    docker.io/dijedodol/simple-socks5-server:latest
    ```
    В конце будет сообщение о том, что образ загружен. ✅

#### Шаг 3.3: Настройка "Правил Дома" и Первый Запуск — Идеальный SOCKS5! 🔑

А теперь самое интересное! Мы запустим наш сервер, но сделаем это *правильно* и *универсально*. Забудь о `-it --rm` — это для "поиграться" и временных запусков. Мы же строим *серьёзное* сооружение, которое будет работать надёжно! Выполняй эту команду также **в директории `~/my_socks5_server`**:

Мы добавим несколько важных штрихов к команде запуска:

* `--name socks5-server`: Дадим нашему контейнеру уникальное имя (`socks5-server`), чтобы потом легко его находить, останавливать, запускать и управлять им. Это как присвоить имя своему питомцу. 🐾 Миленько!
* `-p ХОСТ_ПОРТ:КОНТЕЙНЕР_ПОРТ`: Это "переадресация портов" и очень важный параметр! Он выглядит как `ХОСТ_ПОРТ:КОНТЕЙНЕР_ПОРТ`.
    * **`ХОСТ_ПОРТ` (первое число)**: Это порт на твоём **VPS-сервере**, который будет слушать входящие подключения. Именно этот порт ты будешь указывать в настройках своего браузера или приложения. Ты можешь изменить его на любой свободный порт, например, `8888`, `9999` или любой другой в диапазоне от `1024` до `65535` (старайся избегать общеизвестных портов). Например, `-p 8888:1080`.
    * **`КОНТЕЙНЕР_ПОРТ` (второе число, после двоеточия)**: Это порт *внутри* Docker-контейнера, на котором сам SOCKS5-сервер ожидает соединения. **Этот порт менять не нужно!** Он фиксирован для данного Docker-образа (в нашем случае это `1080`).
    Мы говорим Docker'у, что входящие соединения на **ХОСТ_ПОРТ** твоего VPS-сервера должны быть перенаправлены на **КОНТЕЙНЕР_ПОРТ** внутри Docker-контейнера, где и слушает SOCKS5-сервер. 🚪 Забудь про блокировки! 🔓
* `-d`: Запустим контейнер в *фоновом режиме* (detached mode). Это значит, он будет работать тихо, не занимая твой терминал. Ты сможешь закрыть SSH-сессию, а сервер продолжит работать. Как невидимый дворецкий! 🤫 Без лишнего шума.
* `-e 'SSS_USERNAME=...' -e 'SSS_PASSWORD=...'`: Эти опции устанавливают *переменные окружения* внутри контейнера, которые сервер использует для авторизации.

И самое главное: ***твой логин и пароль***! Придумай что-то *очень* надёжное, не "admin123" или "password"! Используй комбинацию букв, цифр и символов. Это же твой личный пропуск! 🔒 Стой на страже своих данных! 🛡️

```bash
docker run -d \
  --name socks5-server \
  -p 1080:1080 \
  -e 'SSS_USERNAME=ТВОЙ_УНИКАЛЬНЫЙ_ЛОГИН' \
  -e 'SSS_PASSWORD=ТВОЙ_СВЕРХСЕКРЕТНЫЙ_ПАРОЛЬ' \
  dijedodol/simple-socks5-server
```
***Обязательно замени `ТВОЙ_УНИКАЛЬНЫЙ_ЛОГИН` и `ТВОЙ_СВЕРХСЕКРЕТНЫЙ_ПАРОЛЬ` на свои надёжные данные! И запомни их!*** 🧐 Без них вход заказан! ⛔

* **Что ты увидишь**: При успешном запуске команда выведет длинный идентификатор Docker-контейнера, например:
    ```
    c2e4f0c8d1a3b5e7f9a1b3c5d7e9f1a3b5c7d9e1f3a5b7c9d1e3f5a7b9c1d3e5
    ```
    Это значит, что контейнер успешно создан и запущен в фоновом режиме. ✅

Если всё прошло успешно, ты увидишь длинный идентификатор контейнера. Это значит, что твой SOCKS5-сервер уже живёт и дышит! Проверить его статус можно командой (выполняется **в любой директории**):

```bash
docker ps
```
* **Что ты увидишь**: Таблицу со списком запущенных Docker-контейнеров. Ты должен увидеть строку с именем `socks5-server` и статусом, начинающимся с `Up ...` (например, `Up 5 seconds`).
    ```
    CONTAINER ID   IMAGE                                COMMAND                  CREATED          STATUS          PORTS                    NAMES
    c2e4f0c8d1a3   dijedodol/simple-socks5-server   "/usr/bin/dumb-init …"   10 seconds ago   Up 8 seconds    0.0.0.0:1080->1080/tcp   socks5-server
    ```
    Ура, оно работает! 🎉 Миссия выполнена, начинаем праздновать! 🥳

---

### Раздел 4: Автоматизация запуска — Дирижёр Systemd в деле! 🎵

Теперь, чтобы твой SOCKS5-сервер был по-настоящему *независимым* и *самостоятельным*, мы научим его просыпаться вместе с VPS после каждой перезагрузки. Для этого нам нужен `systemd` — это такой "главный дирижёр" на сервере, который управляет всеми системными службами.

Мы создадим специальный файл-инструкцию (`.service` файл) для `systemd`, который будет говорить ему: "Эй, когда сервер запустится, убедись, что мой `socks5-server` тоже работает!".

1.  **Создай файл службы Systemd**: Открой текстовый редактор `nano` для создания нового файла службы в системной директории. Эту команду выполняй **в любой директории**, но файл будет создан в `/etc/systemd/system/`.

    ```bash
    sudo nano /etc/systemd/system/socks5.service
    ```
    * **Что ты увидишь**: Откроется пустой текстовый редактор `nano`, готовый принимать твой код. Если файл уже существует, откроется его содержимое. 📝 Почувствуй себя архитектором! 🏗️

2.  **Вставь содержимое файла службы**: Скопируй и вставь следующее содержимое в открывшийся редактор. *Не забудь снова указать свой логин и пароль* вместо заполнителей!

    ```ini
    [Unit]
    Description=My Awesome SOCKS5 Proxy 🚀
    # Убеждаемся, что Docker и сеть уже работают, прежде чем запускать наш прокси
    After=network-online.target docker.service
    Wants=network-online.target

    [Service]
    # Если наш прокси-сервер почему-то решит вздремнуть (упадёт), systemd его разбудит!
    Restart=always
    # Перед стартом: Если контейнер с таким именем существует, сначала останавливаем и удаляем его,
    # чтобы избежать конфликтов. '|| true' позволяет команде не прерываться, если контейнера нет.
    ExecStartPre=-/usr/bin/docker stop socks5-server
    ExecStartPre=-/usr/bin/docker rm socks5-server
    # *** ИСПРАВЛЕНИЕ: УДАЛЕН ФЛАГ -d ДЛЯ ЗАПУСКА В FOREGROUND РЕЖИМЕ ***
    ExecStart=/usr/bin/docker run --name socks5-server -p 1080:1080 -e SSS_USERNAME=ТВОЙ_УНИКАЛЬНЫЙ_ЛОГИН -e SSS_PASSWORD=ТВОЙ_СВЕРХСЕКРЕТНЫЙ_ПАРОЛЬ dijedodol/simple-socks5-server
    # При остановке службы systemd, останавливаем наш контейнер.
    ExecStop=/usr/bin/docker stop socks5-server

    [Install]
    # Служба будет запускаться, когда система перейдёт в многопользовательский режим.
    WantedBy=multi-user.target
    ```
    * **Сохрани файл**: Нажми `Ctrl+X`, затем `Y` (для подтверждения сохранения), и `Enter` (для подтверждения имени файла). Файл службы успешно сохранен! ✅
    * **Что ты увидишь**: После нажатия `Ctrl+X` внизу редактора появится запрос `Save modified buffer? (Yes/No/Cancel)`. Нажми `Y`. Затем будет запрос `File Name to Write: /etc/systemd/system/socks5.service`. Нажми `Enter`. Ты вернешься в командную строку. ✅

3.  **Сообщи `systemd` о новом файле службы**: После создания или изменения файла службы, `systemd` необходимо перезагрузить, чтобы он "увидел" изменения. Выполняй эту команду **в любой директории**:

    ```bash
    sudo systemctl daemon-reload
    ```
    Это как будто ты говоришь Systemd: "Так, дружище, вот тебе новая инструкция, усвой ее!". 🎼 Он запомнит! 🧠

    * **Что ты увидишь**: Команда обычно не выводит никакого текста при успешном выполнении. Просто вернется приглашение командной строки. ✅

4.  **Включи службу, чтобы она запускалась при загрузке**: Это активирует службу SOCKS5, чтобы она автоматически запускалась каждый раз при старте VPS. Выполняй эту команду **в любой директории**:

    ```bash
    sudo systemctl enable socks5.service
    ```
    Теперь дирижёр знает, что эту мелодию нужно играть всегда! 🎶🔁 На автопилоте! 🤖

    * **Что ты увидишь**: Обычно ничего не выводится, если команда прошла успешно. Иногда может быть сообщение типа `Created symlink /etc/systemd/system/multi-user.target.wants/socks5.service -> /etc/systemd/system/socks5.service.`. Это подтверждает, что автозапуск настроен. ✅

5.  **Запусти службу прямо сейчас**: Чтобы не ждать перезагрузки, запустим её немедленно. Выполняй эту команду **в любой директории**:

    ```bash
    sudo systemctl start socks5.service
    ```
    И пусть музыка играет! 🎸 Полный вперёд! 💨

    * **Что ты увидишь**: Никакого вывода, если служба успешно запущена. ✅

6.  **Проверь статус службы**: Убедись, что всё работает, как часы, и служба успешно запущена. Выполняй эту команду **в любой директории**:

    ```bash
    sudo systemctl status socks5.service
    ```
    * **Что ты увидишь**: Подробную информацию о статусе службы. Главное, чтобы строка `Active:` содержала `active (running)`.
        ```
        root@v55748:~/my_socks5_server# sudo systemctl status socks5.service
        ● socks5.service - My Awesome SOCKS5 Proxy 🚀
             Loaded: loaded (/etc/systemd/system/socks5.service; enabled; vendor preset: enabled)
             Active: active (running) since Thu 2025-06-05 10:37:25 MSK; 9s ago
           Process: 129415 ExecStartPre=/usr/bin/docker stop socks5-server (code=exited, status=0/SUCCESS)
           Process: 129423 ExecStartPre=/usr/bin/docker rm socks5-server (code=exited, status=0/SUCCESS)
             Main PID: 129429 (docker)
             Tasks: 6 (limit: 1012)
             Memory: 8.1M
                CPU: 116ms
             CGroup: /system.slice/socks5.service
                     └─129429 /usr/bin/docker run --name socks5-server -p 1080:1080 -e SSS_USERNAME=m35 -e SSS_PASSWORD=Maxdiamond4 dijedodol/simple-socks5-server
        ```
        Ты должен увидеть **`Active: active (running)`**. Это означает, что твой SOCKS5-сервер бодрствует и готов к работе! 🎉 Строки после этой информации (начинающиеся с `Jun 05 ... docker[PID]:`) показывают внутренние логи работы SOCKS5-сервера, в том числе, что он начал слушать на порту. Нажми `q` для выхода из просмотра статуса.

---

### Раздел 5: Таблица Важных Команд — Шпаргалка для Гуру Прокси! 📚

Чтобы ты всегда был на коне и мог управлять своим SOCKS5-сервером одной левой (или правой, кому как удобно), вот тебе удобная табличка с самыми нужными командами. Все эти команды можно выполнять **в любой директории** на твоём VPS.

| Действие | Команда | Описание |
| :-------------------------------------- | :--------------------------------------------------- | :-------------------------------------------------------------------------------------- |
| **Проверка статуса контейнера Docker** | `docker ps` | Показывает запущенные Docker-контейнеры. Ищи `socks5-server`! 👀 |
| **Остановка контейнера Docker** | `docker stop socks5-server` | Усыпляет твой SOCKS5-сервер. 😴 |
| **Запуск контейнера Docker** | `docker start socks5-server` | Будит сервер, если он был остановлен вручную. ☀️ |
| **Перезапуск контейнера Docker** | `docker restart socks5-server` | Останавливает и снова запускает контейнер. 🔄 |
| **Проверка статуса службы Systemd** | `sudo systemctl status socks5.service` | Показывает, работает ли твой "дирижёр" и прокси-сервер. ✅ |
| **Запуск службы Systemd** | `sudo systemctl start socks5.service` | Запускает службу SOCKS5 (и контейнер Docker). ▶️ |
| **Остановка службы Systemd** | `sudo systemctl stop socks5.service` | Останавливает службу SOCKS5 (и контейнер Docker). ⏸️ |
| **Перезапуск службы Systemd** | `sudo systemctl restart socks5.service` | Перезапускает службу SOCKS5. 🔁 |
| **Включение автозапуска службы Systemd**| `sudo systemctl enable socks5.service` | Делает так, чтобы сервер сам просыпался после перезагрузки VPS. ⚡ |
| **Отключение автозапуска службы Systemd**| `sudo systemctl disable socks5.service` | Запрещает серверу автоматически запускаться. 🚫 |
| **Просмотр логов службы Systemd** | `sudo journalctl -u socks5.service -f` | Показывает "дневник" работы твоего прокси-сервера в реальном времени. 📜 |
| **Открытие порта 1080 в UFW** | `sudo ufw allow 1080/tcp` | Разрешает внешним подключениям добираться до твоего прокси. 🔓 |
| **Проверка статуса UFW** | `sudo ufw status` | Показывает текущие правила твоего фаервола. 🛡️ |

---

### Раздел 6: Последние Штрихи и Тестирование — Победа! 🏆

Твой SOCKS5-сервер теперь не только установлен, но и полностью автономен! Поздравляю! 🎉

* **Проверка контейнера:**
    Убедись, что Docker-контейнер действительно запущен. Выполняй эту команду **в любой директории**:

    ```bash
    docker ps
    ```
    * **Что ты увидишь**: Таблицу, аналогичную той, что была показана после первого запуска (Шаг 3.3), с контейнером `socks5-server` и статусом `Up ...`. ✅

* **Тестирование прокси:**
    Теперь ты можешь настроить свой браузер, мессенджер или другое приложение на использование SOCKS5-прокси.

    * **Адрес:** Публичный IP-адрес твоего VPS-сервера. 🌐
    * **Порт:** `1080` (или тот, который ты указал, если вдруг решил изменить). 🔢
    * **Тип:** SOCKS5. 🕵️‍♂️
    * **Авторизация:** Твой `ТВОЙ_УНИКАЛЬНЫЙ_ЛОГИН` и `ТВОЙ_СВЕРХСЕКРЕТНЫЙ_ПАРОЛЬ`. 🔑

* **Настройка фаервола (ОЧЕНЬ ВАЖНО!):**
    Если у тебя включен фаервол (например, `ufw` или `firewalld`), не забудь открыть порт `1080`, иначе твой сервер будет невидим для внешнего мира! 🚫🌍 Выполняй эти команды **в любой директории**:

    Пример для `ufw`:

    ```bash
    sudo ufw allow 1080/tcp
    sudo ufw enable # Если ещё не включен, будь осторожен!
    ```
    * **Что ты увидишь**:
        * После `sudo ufw allow 1080/tcp`: `Rule added` или `Rules updated`. ✅
        * После `sudo ufw enable`: `Command may disrupt existing ssh connections. Proceed with operation (y|n)?` (нажми `y` и `Enter`), затем `Firewall is active and enabled on system startup`. ✅
        Команда `sudo ufw enable` *включает* фаервол. Если он у тебя уже включен, не используй её повторно, чтобы случайно не заблокировать себе доступ. Всегда сначала проверяй статус `sudo ufw status`. Держи свою крепость под контролем! 🏰🔥

Вот и всё! Твой SOCKS5-сервер теперь не просто стоит, он *живет* на твоём VPS и готов служить тебе верой и правдой, даже после перезагрузки. Ты справился! Теперь иди и наслаждайся своим новым уровнем свободы в интернете! 🚀
